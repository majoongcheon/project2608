# 발표 슬라이드 — 신지현 담당 영역

**작성** 2026-09-04 · 신지현 세션 (2차 개정)
**용도** 슬라이드에 그대로 옮길 문장. 원안의 항목 순서를 그대로 지켰다.

> 수치는 2026-09-04 오후 기준 실측. 확인 명령은 맨 아래.

---

## 슬라이드 원문 (이대로 옮기면 된다)

### 저장 위치

- **AI 소통방 · 마이페이지 — 내 브라우저에만 저장**
  서버로 보내지 않습니다.
  단, 안내봇이 **답하지 못한** 질문은 그 문장만 따로 받습니다.
  답해 드린 질문은 보내지 않고, 누가 물었는지도 받지 않습니다.
- **후기방 — 서버에 저장**
  글이 공유되어야 '소통'이 되기 때문입니다.
  이름·연락처·IP·위치는 **컬럼 자체를 만들지 않았습니다.**

### AI 소통방 규칙 구축 과정

- 피드백 — "모르겠습니다" → **0개 (0%)**
  화면에 **없는** 질문 50개로 시험. 시작할 때는 34개(68%)였습니다.
- 규칙 **94개**
- 화면 질문 **112개**
- 프론트 테스트 **56개**

### 주로 맡은 영역

프로젝트 기획 · 웹사이트 설계 및 UI 디자인 · AI 안내봇 설계 및 규칙 구축 ·
데이터 준비 및 모델 비교·검증 · 원본 507컬럼 군집 분석 및 EDA 구성

### 발표에 들어간 결과물

데이터 준비 · 모델 11구성 비교 · 원본 507컬럼 군집화 · 홈페이지 구축

---

## 항목별 뒷받침 (질문 들어올 때 꺼낼 것)

| 항목 | 근거 |
|---|---|
| 데이터 준비 | `SJH-데이터준비와-모델선정.md` — 뷰 DDL 을 DB 에서 직접 떠 대조, **문서↔구현 불일치 4건 정정** |
| 모델 11구성 비교 | `ml/SJH-model-benchmark.py` · `SJH-모델비교-결과.json` — 11구성 × 2문항셋 = **22행**. LightGBM·XGBoost·CatBoost 포함, 다항 로지스틱 채택 재확인 |
| 원본 507컬럼 군집화 | Gower 거리 · PAM · 계층군집 **직접 구현** → **최적 k=3**, 널 대비 27배로 구조 있음 |
| EDA 구성 | `notebooks/SJH-eda-학습데이터셋_최종.ipynb` |
| 홈페이지 구축 | 다섯 방 구조 · 홈 에디토리얼 전면 개편 · 시계 문자판 척도 · 판정 불가 화면. `TalkPage`·`ReviewPage`·`MyPage`·`guideBot` **최초 작성자** |
| AI 안내봇 | 규칙 94 · 화면 질문 112 · 무응답 68% → 0% |

**커밋 82건** (세션 장부로 귀속 확인된 것)

---

## 원안에서 고친 곳

| 원안 | 고침 | 왜 |
|---|---|---|
| 프론트 테스트 **52번** | **56개** | 신고·미답변 질문 테스트가 늘었습니다 |
| "AI 소통방은 내 브라우저에만" | **예외 한 줄 추가** | 오늘 오후부터 답 못 한 질문은 서버로 갑니다. **화면에서는 이미 밝히고 있어서**, 발표에서 빠지면 어긋납니다 |
| 규칙 **훈련** 과정 | 규칙 **구축** 과정 | 학습 모델이 아닙니다. 이 말이 있으면 "무슨 데이터로 훈련했냐"가 곧바로 따라옵니다 |

**EDA 구성은 원안대로 넣었습니다.** 09-01 세션은 공용 계정으로 커밋되어 저장소만으로는
가릴 수 없었고, 본인 확인(2026-09-04)으로 귀속이 확정되었습니다. 작업기록 §13 에 남겼습니다.

---

## 말로 덧붙일 것

**저장 위치** — "예외를 굳이 말씀드리는 이유는, 화면에서도 이용자에게 그대로 말하고
있기 때문입니다. 숨기면 서비스가 이용자에게 거짓말을 하게 됩니다."

**규칙 구축** — "68%에서 0%로 줄인 것보다 **재고 시작했다는 점**을 봐 주시면 좋겠습니다.
'더 늘릴 수 있나'는 재기 전에는 답할 수 없는 질문이었습니다."

**군집화** — "라이브러리를 쓰지 않고 Gower 거리와 PAM 을 직접 구현했습니다. 결측이
'모름'이 아니라 **설문 분기 구조**라는 것을 확인해야 했고, 기성 구현은 그걸 못 합니다."

---

## 수치 확인 명령

```bash
grep -n "BOT_STATS" frontend/src/services/guideBot.ts   # 규칙 94 · 질문 112
cd frontend && npm test                                  # 56
git log --all --format='%h' --author="신지현" | wc -l     # 82
```
