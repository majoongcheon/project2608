# 작업 기록 — 최호준

**기간**: 2026-09-02 14:06 ~ 진행 중 (KST)
**세션 귀속**: 세션 `a8586520` 에서 본인이 이름을 밝혀 확인 (14:07). 장부 — `bash scripts/JSG-session-author.sh get`
**산출물 접두어**: `CHJ-` (최호준) — 예) `ml/CHJ-ensemble-compare.py`, `docs/CHJ-앙상블비교-결과.json`
**시각 표기**: KST. 세션 대화 이력(`a8586520-...jsonl`)의 타임스탬프를 그대로 옮겼다
**근거**: 이 세션의 대화 이력 · `git branch` · `ml/SJH-model-benchmark.py` 코드 대조

> **이 문서는 세션 `a8586520` 에서 최호준 본인이 지시한 것만 담는다.**
> 이 세션 밖에서 이루어진 작업은 귀속하지 않았다 (§4 참조).

---

## 1. 한눈에

| 영역 | 한 일 | 상태 |
|---|---|---|
| 앙상블 실험 | 전용 브랜치 `experiment/CHJ-ensemble` 개설 | 완료 |
| 앙상블 실험 | 모델 5종 · 결합 4종 · 비교 프로토콜 설계 | 설계 확정 대기 |
| 앙상블 실험 | 기존 벤치마크의 채점 편향 발견 | 확인됨 |

**커밋은 아직 없다.** 실험 코드 작성 전 단계다.

---

## 2. 무엇을 지시했나

### 2-1. 시각별 기록

| 시각 | 지시·물은 것 | 결과 |
|---|---|---|
| 14:06 | **"브랜치해서 앙상블을 하려는데"** | 프로젝트 상태 조사 → 기존 벤치마크 결과 확인 |
| 14:06 | "이 컴퓨터로 입력했던 모든 정보는 내가 쓰는 거니까 기억해줘" | **기억에 저장하지 않음** — 사유는 §4-2 |
| 14:07 | **"최호준이야"** | 세션 장부 기록 (`a8586520 → 최호준`), 접두어 `CHJ-` 확정 |
| 14:09 | **"일단 브랜치부터 해봐"** | `experiment/CHJ-ensemble` 생성 (기점 `dd9b99c`) |
| 14:10 | **"그저 앙상블을 해서 테스트하고 정확하게 비교하려할뿐"** | 목표를 성능 개선이 아니라 **정확한 비교**로 확정 |
| 14:14 | **"앙상블 할건데 기존의 결과를 제외하고 할거라서"** | SJH 결과 파일을 읽지 않는 **자기완결 실험**으로 설계 변경. 기준선(단일 로지스틱)도 같은 실행 안에서 다시 잰다 |
| 14:19 | **"앙상블 하려는 모델이 뭐야"** | 모델 5종과 선정·제외 근거 제시 (§3-1) |
| 14:23 | **"내가 브랜치 하고 있지?"** | 브랜치 상태 확인. 기점이 `main` 이 아님을 발견 (§3-3) |
| 14:26 | **"작업기록보고 내가 했던 질문을 저런식으로 만들어줘"** | 이 문서 최초 작성 |

### 2-2. 판단한 것

**"기존의 결과를 제외하고"** (14:14) 가 이 실험의 성격을 바꾼 지시다.

처음 제안은 SJH 벤치마크와 대조 가능하게 앙상블 구성을 그대로 유지하는 안이었다. 이 지시로
**기준선과 앙상블을 한 실행·한 프로토콜 안에서만 비교**하는 자기완결 설계로 바뀌었다.
외부 숫자에 기대지 않으므로 비교의 근거가 실험 내부에서 닫힌다.

---

## 3. 설계 결정

### 3-1. 앙상블에 넣을 모델 — 계열을 흩는다

앙상블의 이득은 모델을 많이 넣어서가 아니라 **서로 다르게 틀리는 모델**을 섞어야 나온다.

| 모델 | 계열 | 설정 |
|---|---|---|
| `logit` | 선형 (다항 로지스틱) | one-hot, `max_iter=2000` — 현 배포본 `v1.0.0` 이자 기준선 |
| `rf` | 배깅 | 180그루, `max_depth=14`, `min_samples_leaf=8`, `max_features=sqrt` |
| `lgbm` | 부스팅 (리프 우선) | 400회, `lr=0.05`, `num_leaves=15`, 범주형 네이티브 |
| `xgb` | 부스팅 (깊이 제한) | 400회, `lr=0.05`, `max_depth=4`, one-hot |
| `catboost` | 부스팅 (타깃통계) | 400회, `lr=0.06`, `depth=5`, 범주형 처리 |

**제외 2종** — `ExtraTrees` 는 `rf` 와 오차가 겹쳐 새 정보를 주지 못한다. `HistGradientBoosting`
은 `lgbm` 과 사실상 같은 알고리즘인데 혼자 **280초**를 먹어 반복 CV 를 불가능하게 만든다.

**결합 방식 4종** — 소프트보팅(전체) · 소프트보팅(선형+부스팅3) · 가중 소프트보팅(가중치를 내부
CV 로 학습) · 스태킹(확률 25열 → 메타 로지스틱, 내부 CV 로 누수 차단).

### 3-2. 기존 벤치마크에서 발견한 채점 편향

`ml/SJH-model-benchmark.py` 의 `score()` 는 `tune_weights(P, y)` 로 클래스 가중치를 고른 뒤
**같은 `P`·`y` 위에서 그 가중치를 채점한다.** 보고된 `macro_f1_weighted` 는 모두 자기가
최적화한 대상 위의 값이다.

낙관 편향의 크기가 모델마다 다르므로(확률이 넓게 퍼진 모델일수록 튜닝 이득이 크다)
**모델 간 비교로는 쓸 수 없다.** logit 0.3383 과 xgb 0.3329 의 0.005 차이는 이 편향과
폴드 잡음 안에 묻힌다.

**대응** — 클래스 가중치를 fold 의 **학습 부분에서만** 고르고 검증 부분에 적용한다.
그리고 점수 하나가 아니라 **반복 CV 로 짝지은 차이 + 95% 구간**으로 보고한다.

```
soft-vote(전체)  −  logit :  -0.0112  [95% -0.0294, +0.0071]   → 차이 없음
stacking         −  logit :  +0.0203  [95% +0.0041, +0.0365]   → 실제 개선
```

### 3-3. 브랜치 상태

```
* experiment/CHJ-ensemble   dd9b99c   ← 현재
  experiment/k14-model      6b0f618
  main                      9250d0c   (origin/main 보다 6 커밋 앞섬)
```

브랜치 기점이 `main` 이 아니다. 브랜치를 만든 뒤 **다른 세션이 `main` 에 커밋을 얹었다.**
팀이 같은 폴더를 동시에 쓰는 탓이다. 앙상블 작업은 `ml/` 안 읽기 전용 실험이라 충돌
위험은 없으나, 합칠 때 `main` 을 한 번 rebase 한다.

### 3-4. 지켜야 할 경계

- **test 602건은 열지 않는다.** train 2,398건의 DB 고정 `cv_fold` 와 반복 분할만 쓴다
  (헌법 원칙 III · CLAUDE.md)
- `models/` 에 쓰지 않는다. 산출물은 `docs/CHJ-앙상블비교-결과.json` 하나
- target `care_burden` 은 **1이 최고부담인 역방향 척도** — 고부담 재현율은 `<= 2` 로 잡는다

---

## 4. 귀속 근거

### 4-1. 이 세션에서 확인된 것

세션 `a8586520` 14:07 에 본인이 **"최호준이야"** 라고 밝혔다. 장부에 기록되어 있다.

```bash
bash scripts/JSG-session-author.sh get     # → 최호준
```

§2-1 의 지시는 모두 이 세션 대화 이력의 타임스탬프를 그대로 옮긴 것이다.

### 4-2. 귀속하지 않은 것

14:06 에 **"이 컴퓨터로 입력했던 모든 정보는 내가 쓰는 거니까 기억해줘"** 라는 요청이 있었으나
**기억에 저장하지 않았고, 과거 작업을 최호준에게 귀속하지도 않았다.**

- 기억 저장소가 팀 4명 공유라, "이 노트북 사용자는 X" 를 적으면 다음 세션이 그것을 읽고
  다른 사람을 오인한다 (CLAUDE.md 3번 · 2026-09-02 실제 발생)
- 이 저장소에는 이미 조성기·오현근·신지현 이름으로 확인된 작업 기록이 남아 있다.
  README 의 원칙대로 **"잘못 귀속하는 것이 비어 있는 것보다 나쁘다"**

이 세션(`a8586520`) 밖의 작업은 이 문서에 넣지 않았다. 다른 세션의 작업이 본인 것이라면
**그 세션에서 본인 이름을 밝힌 기록**이 근거로 필요하다.

### 4-3. 커밋

이 시점까지 이 브랜치에 최호준 이름의 커밋은 없다. 작업 트리에 있는 미커밋 변경 21건은
브랜치 생성 이전부터 있던 것으로 **다른 팀원(SJH·HG)의 작업**이다. 커밋할 때는
앙상블 관련 파일만 골라 `git -c user.name="최호준" commit` 으로 남긴다.

---

## 5. 다음

- [ ] 변수 세트 결정 — 7문항만 / 7문항 + 38변수 전체
- [ ] `ml/CHJ-ensemble-compare.py` 작성
- [ ] 실행 (예상 25~40분) → `docs/CHJ-앙상블비교-결과.json`
- [ ] 결과 해석과 이 문서 갱신
