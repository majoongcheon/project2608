# 작업 기록 — 조성기

**기간**: 2026-08-31 ~ 2026-09-01 (1일차 마감)
**근거**: 이 세션(노트북)에서 실제로 지시하고 수행한 작업. 아래 "귀속 근거" 참조.

---

## 한눈에

| 영역 | 한 일 |
|---|---|
| **명세 정제** | 외부 검토 13건을 spec.md 에 반영 → FR **16개 신설** |
| **설계** | plan · research · data-model · API 계약 · quickstart **5종 산출** |
| **작업 분할** | tasks.md **98개 작업** 으로 분해 |
| **구현** | 백엔드 · 프론트엔드 · ML 파이프라인 · DB 계층 **전 계층 골격** |
| **모델** | 38변수 → **7문항 v1.0.0** 도출, SC 게이트 4개 통과 |
| **배포** | 포트·도메인 매핑, `check_project.sh`, nginx 설정 |
| **팀 협업** | 수정 요청 수집 체계 구축, 지도 교체 반영(JSG-01) |
| **팀 기록 체계** | 수정 요청 수집(`docs/feedback/`) · 팀원별 작업 기록(`docs/작업기록/`) |
| **발표 준비** | 개발 수정이력 · 발표자료 18슬라이드 · 차트 9종 |

---

## 1. 명세 정제 — 외부 검토 반영 (8/31~9/1)

`docs/chatgpt_검토결과.pdf` 의 지적 13건을 spec.md 와 대조해 **전부 반영**했다.

**본인이 판단한 것** — 검토 의견 중 두 건은 결정이 필요했고, 선택지를 받아 직접 골랐다.

| 항목 | 선택 |
|---|---|
| 기여 요인 "최소 3개" | **최소 3개 유지 + 약한 요인 구분 표현** (완화안 대신) |
| 접근성 전면 제외 | **기본 사용성 4건은 범위에 유지** (FR-039~042 신설) |

접근성 결정은 원래 "실 서비스 계획 없음"을 이유로 제외했던 것을 뒤집은 것이다.
대상이 발달장애인 보호자이고 고부담을 경고 색상으로 표시한다는 점이 근거였다.

**결과** — FR 110개, Clarifications 2개 세션(문답 38건), 신설 FR 16개
(FR-008-1·008-2·008d·008e·011-1·013c-1·021-1·021-2·021j-1·036a~c·039~042)

---

## 2. 설계 — Spec Kit 전 단계 진행 (9/1)

`/speckit.plan` → `/speckit.tasks` → `/speckit.implement` 를 순서대로 지시하고 산출물을 받았다.

### 2-1. 계획 (`/speckit.plan`)

**본인 지시**: spec.md 와 `Intent-Plan.md` 를 참조하고, `notebooks/eda-학습데이터셋_최종.ipynb`
의 검토 내용을 반영해 **데이터·모델 구성 계획을 추가**할 것.

| 산출물 | 내용 |
|---|---|
| `plan.md` | Technical Context · Constitution Check(1·2차) · 데이터/모델 구성 계획 |
| `research.md` | 기술 결정 11건 (계열 선택, 런타임 추론, 기여도, 문항 선별, 임계값 등) |
| `data-model.md` | 학습 3 + 운영 9 + 관측 2 + 클라이언트 3 |
| `contracts/openapi.yaml` | 엔드포인트 10개 · 스키마 15개 |
| `quickstart.md` | 개발 · 학습 · 테스트 · 배포 절차 |

**이 단계에서 DB 실측으로 계획을 검증**했고, 예상 못 한 자산 둘을 발견했다 —
`cv_fold` 가 5-fold 로 이미 배정되어 있었고, `v_cb_tree_v1`·`v_cb_linear_v1` 두 뷰가
결측 처리를 정반대로 준비해 두고 있었다. 후자가 "계열을 사전에 정하지 않고 벤치마크로
고른다"는 결정의 근거가 됐다.

### 2-2. 작업 분할 (`/speckit.tasks`)

**본인 지시**: `Intent-Tasks.md` 를 참조해 단계별로 분할할 것.

- **98개 작업**, User Story 별 구성 (US1 진단 36 · US2 지도 14 · US3 연계 11)
- `Intent-Tasks.md` 가 지정한 디자인 템플릿(`npx getdesign@latest add airbnb`)을
  T007 로 배치하고 **모든 화면 작업의 선행 조건**으로 지정

---

## 3. 구현 — 전 계층 골격 (9/1)

**본인 지시**: 포트는 프론트/백엔드 각각 지정, 도메인 `p3.sumzip.com` 매핑,
`check_project.sh` 로 시작·중지·재시작 가능하게, **서비스가 완전히 작동할 때까지**.

| 계층 | 결과 |
|---|---|
| 백엔드 | Node 20 + Express + TS · 20파일 · API 10개 |
| 프론트엔드 | Vue 3 + Vite + Pinia · 22파일 · 화면 7개 |
| ML | Python 오프라인 파이프라인 5파일 |
| DB | 마이그레이션 3 + 시드 5 + 점검 스크립트 |
| 운영 | `check_project.sh` (start/stop/restart/status/check/setup/logs) |

**실측 데이터로 구축했다** — 시연용 목 데이터 없이 전부 실제 DB(`ABC8pioneer3`)를 쓴다.
기관 1,283행을 875개 기관 + 1,277 서비스로 정규화하고, 전국 229개 시군구 목록을 세워
**미확보 8개 지역을 특정**했다(대구 군위, 인천 강화·옹진, 경기 오산·가평, 전남 장성, 경북 고령·울릉).

### 실행 중 드러난 결함 7건

문서만으로는 보이지 않고 돌려봐야 드러난 것들이다.

| # | 결함 | 처리 |
|---|---|---|
| 1 | 고부담 재현율 0.678 — SC-005 미달 | 배제 변수를 되살리지 않고 **결정 가중치** 도입 → 0.773 |
| 2 | 선택 계열은 logit인데 export는 RandomForest | 계열별 분기 |
| 3 | 후진 제거가 한 개도 못 떨굼 | logit 중요도 계산 추가 |
| 4 | CV 최소 k가 test에서 SC-004 초과 | 마진 도입 + 동일 조건 비교 |
| 5 | 미확보 지역에 다른 지역 기관이 섞임 | 자동 확대 제거 |
| 6 | FR-032 "공통 컬럼 0개"가 과한 표현 | 검사·문서를 의도에 맞게 수정 |
| 7 | `/facilities` 가 좌표 미반환 → 마커 렌더 불가 | 카드에 좌표·주소 추가 |

---

## 4. 모델 — 7문항 v1.0.0 (9/1)

38개 설명변수에서 **후진 제거**로 7문항을 도출하고 SC 게이트 4개를 통과시켰다.

| 지표 | 결과 | 기준 |
|---|---|---|
| macro F1 | 0.3416 (기준 모델 대비 손실 0.0200) | ≤0.03 ✅ |
| 고부담 재현율 | 0.7730 | ≥0.70 ✅ |
| 판정 불가 | 2.5% | ≤10% ✅ |
| Python↔TS 패리티 | 3,000건 1e-9 이내 | 릴리스 게이트 ✅ |

**계열 비교** — 다항 로지스틱(0.3180) > 랜덤 포레스트(0.2894) > 엑스트라 트리(0.2668)

---

## 5. 배포 (9/1)

- 포트를 **9513/9533 → 9503/9523** 으로 재배정, 21곳 일괄 수정
- **`p3.sumzip.com` 502 원인 규명** — nginx 에러 로그에서 `SSL_do_handshake() failed
  (wrong version number)` 확인. 포트가 아니라 **스킴이 https 인 것**이 원인이었다.
  재현으로 확정하고 설정·설치 스크립트(`deploy/`)를 준비
- 공유 nginx(vhost 82개)라 `p03.conf` 하나만 건드리는 범위로 한정

---

## 6. 팀 협업 체계 (9/1)

**본인 지시**: 수정 사항을 팀원별로 따로 모아 두었다가 종합해 반영할 것.

- `docs/feedback/` 구축 — 팀원당 파일 하나, **원문 그대로 보존** + 분류·상태·메모 분리
- 종합 시 확인할 것을 미리 정의 (팀원 간 충돌 / 명세·헌법 충돌 / 모델 재학습 필요 / 반영 순서)

### 본인이 제기한 수정 요청 2건

| ID | 요청 | 상태 |
|---|---|---|
| **JSG-01** | 카카오 키 없이 지도 시각화 (folium 예시 참조) | ✅ **반영** (`b2e87a0`) |
| **JSG-02** | 학습 이용 동의 문항이 강요처럼 느껴짐 | ⏸ 팀 논의 중 |

**JSG-01 반영** — Kakao SDK → Leaflet + OpenStreetMap. 중심좌표는 **현재 위치 또는
선택 지역 대표 좌표**로 유지하라는 본인 판단을 반영(예시의 마커 평균 방식은 채택 안 함).
작업 중 위 결함 7번(좌표 미반환)을 함께 발견해 고쳤다.
`research.md` **R-7 결정 번복**을 원래 판단과 함께 기록.

**JSG-02 제기** — 팀에 문제를 제기해 둔 상태이며 배치 방식 미확정.
"팝업이 문제가 아니라 **강제 관문**이 문제"라는 원인 분석까지 정리되어 있다.

---

## 7. 발표 준비 (9/1)

### 7-1. 기록 문서

| 산출물 | 내용 |
|---|---|
| `docs/개발-수정이력.md` | 명세→구현 과정의 수정 이력, 발표용 사례 5선 |
| `docs/작업기록/` | 팀원별 기여 기록 체계 + 인덱스 |

**git 작성자로 팀원을 구분할 수 없다는 것을 확인**하고(모든 커밋이 `pioneer3` 단일 작성자),
세션 이력을 근거로 삼는 방식과 이름 대기 목록을 만들었다.

### 7-2. 발표자료 — 18슬라이드

| 산출물 | 내용 |
|---|---|
| `docs/발표자료.md` | 데이터 분석 · 문항 선정 기준 · 돌봄 서비스 · 서비스의 의미 |
| `docs/발표자료.pptx` | 18슬라이드, 편지 톤 |
| `docs/assets/` | 차트 9종 (200dpi) |
| `ml/chart_data.py` · `make_charts.py` · `make_map.py` · `make_pptx.py` | 재생성 스크립트 |

**본인이 지시한 구성** — `notebooks/`·`docs/` 의 분석 결과를 근거로 삼고,
문항 선정 기준·국내 돌봄 서비스·서비스의 의미를 함께 담을 것.

### 7-3. 시각화 — 요청과 판단

차트 후보를 추천받아 **A~G 전부와 대전 지역 지도**를 채택했다.

| 차트 | 내용 |
|---|---|
| A | 부담 분포 100% 누적 막대 — 고부담 54.3% 를 한눈에 |
| B | **k별 혼동행렬** (2×2 + 5×5) — 문항 선정 기준의 근거 |
| C | 문항 수별 성능 곡선 (k=38→6) |
| D | 설명력 순위 + 배제 변수 강조 |
| E | 군집화 — 소형 다중 패널 + 실루엣 |
| F | 7문항 vs 14문항 지표 4종 |
| G | 연령–서비스 사각지대 다이어그램 |
| H | **대전광역시 기관 34곳 지도** (OSM 타일) |

**본인이 판단한 것**

- **혼동행렬을 문항 선정 근거로 쓸 수 있는지 물었다.** 직접적인 기준은 아니지만
  "기준이 옳았는지"의 증거가 된다는 답을 받고 채택했다. 실제로 계산해 보니
  **k=6 에서 최고부담군 재현율이 0.183 → 0.054 로 무너지는 것**이 드러나,
  "F1 이 떨어져서"보다 훨씬 설득력 있는 정지 근거가 나왔다.
- **부담 분포를 누적 막대로 바꾸라**고 지시 — 표로는 54.3% 가 글로만 전달됐다.
- **대전 지역 지도**를 요청 — 전국 커버리지 지도는 96.5% 라 차이가 안 보인다는 판단을 받아들이고
  현재 위치인 대전으로 좁혔다.
- **색 구분이 안 된다고 지적하고 가독성 우선을 지시했다.** "브랜드 톤을 맞춘 것은 좋지만
  그래프에서 구분이 힘들면 의미가 없다"는 기준을 제시했다.
- **11페이지 여백을 지적**하고 그래프와 표를 균형 있게 배치하도록 지시했다.

### 7-4. 색 팔레트 — 검증으로 교체

지적을 받아 팔레트를 **검증 도구로 계산**해 교체했다.

| 팔레트 | 정상시야 ΔE | 판정 |
|---|---:|---|
| 기존 살구 5색 램프 | 10.1 | **FAIL** (기준 15) |
| 새 2색 (주황·파랑) | 33.6 | ALL PASS |
| 새 3색 (+초록) | 24.0 | ALL PASS |

원인은 **순차 램프(단일 색조)를 식별 용도로 쓴 것**이었다.
바탕·글자는 편지 톤을 유지하고 **데이터 마크만** 교체했다.
산점도는 색 5개 대신 **패널 5개**로 바꿔 구분 문제를 없앴다.

## 귀속 근거 — 읽는 사람을 위한 주의

**git 작성자로는 팀원을 구분할 수 없다.** 이 저장소의 모든 커밋 작성자가
`pioneer3 <vrmaker65@gmail.com>` 하나다. 같은 폴더를 여러 세션이 공유하기 때문이다.

따라서 이 문서는 **git log 가 아니라 이 세션의 대화 이력**을 근거로 한다.
같은 기간에 **다른 세션에서 수행된 작업**이 있으며, 아래는 이 세션 밖의 것이라
조성기의 기여로 적지 않았다.

- 14문항 `v1.1.0` 실험과 7문항 복귀 (`1e4144a` `c59b01d` `59ac586`)
- 디자인 템플릿 교체 — Notion → claude 편지 톤, 홈 화면 재설계
  (`6642b82` `68e7a78` `5e5a130` `ac954d3` `8e7321c` `09229ff` `7dda352` `2d064fe` `81b0609` 등)
- 모델 문서 4종 (`모델-쉽게-설명` `모델-학습-방법` `모델-성능-리포트` `실험기록-문항수와-판정불가`)
- 노트북 4종 (`모델-학습-과정` `모델-성능-리포트` `실험-군집화` `EDA-7문항`)
- 설문 화면 무한 로딩 수정 (`da53d0a`)

**이 구분은 팀장 조성기가 확인했다(2026-09-01).** 위 작업들은 **다른 팀원이 지시한 것**이
맞으며, 각 팀원이 자기 노트북에서 이름을 밝히면 해당 팀원 파일로 옮겨 적는다.

**앞으로** — 2026-09-01 이후 각 노트북(세션)에서 팀원이 이름을 입력하기로 했다.
그 시점부터는 세션 단위로 기여가 자동 구분된다.
워크트리·클론을 나눌 때 `git config user.name` 도 팀원별로 다르게 두면
`git log --author` 로도 집계할 수 있다.

---

## 오늘 마감 시점 상태 (2026-09-01)

| 항목 | 상태 |
|---|---|
| 서비스 | 가동 중 — 프론트 9503 · 백엔드 9523 · 모델 `v1.0.0` 7문항 |
| 도메인 | `p3.sumzip.com` — **nginx 설정 설치 대기** (sudo 필요, `deploy/deploy.sh` 준비됨) |
| 테스트 | 백엔드 44 · 프론트 11 통과 |
| 발표자료 | 18슬라이드 · 차트 9종 완료 |
| 팀 요청 | JSG-01 반영 완료 · JSG-02 팀 논의 중 |

### 내일 이어서 할 것

1. **nginx 설정 설치** — `bash deploy/deploy.sh` (sudo 없이 실행, 비밀번호 창이 뜬다)
2. **JSG-02 배치 방식 확정** — 팀 논의 결과가 나오면 반영
3. **자격증명 정리** — `Intent-Plan.md`·노트북의 평문 접속 정보 (T098)
4. **팀원별 작업 기록 채우기** — 각자 노트북에서 이름을 밝히면 대기 목록에서 이관
