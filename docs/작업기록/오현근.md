# 작업 기록 — 오현근

**기간**: 2026-09-01 (1일차) · 2026-09-02 (2일차)
**근거**: 이 세션(노트북)에서 직접 지시하고 확인한 작업. 아래 "귀속 근거" 참조.
**산출물 접두어**: `HG-` (현근)

---

## 한눈에

| 영역 | 한 일 | 결과 |
|---|---|---|
| **모델 검증** | 학습·교차검증·test 성능을 혼동행렬까지 전수 확인 | 성능 리포트 확정 |
| **문항 수 실험** | 7 → 14문항으로 늘려 실측 비교 | **7문항 유지 결정** |
| **한계 규명** | y 없이 군집화해 데이터 구조를 확인 | **"정확도 48%는 모델 탓이 아님" 입증** |
| **판정 불가 규명** | 왜 뜨는지, 줄일 수 있는지 4가지 가설 검증 | 3개 기각, 원인 확정 |
| **서비스 버그** | 학습값이 배포에 반영 안 되던 문제 발견·수정 | **판정 불가 83→72명, 꺼져 있던 조건 복구** |
| **보안** | 노트북에 박힌 DB 비밀번호 분리 | `.env` 체계 도입 |
| **문서·발표 자료** | 설명 문서 3종, 실행 노트북 4종 | 발표 근거 확보 |

---

## 1. 발표에 바로 쓸 수 있는 세 가지

### ① "왜 정확도가 48%인가" — 데이터로 답했다

가장 나올 만한 질문이다. **정답을 가리고 군집화**해서 답을 만들었다.

| 지표 | 실측 | 뜻 |
|---|---|---|
| 실루엣 계수 | **0.12** | 0.25 미만 = 뚜렷한 덩어리 구조 없음 |
| 부담 구간과 일치도 (ARI) | **0.027** | 0에 붙음 = 거의 무관 |
| PCA 1번 성분 | **12.9%** | 데이터를 관통하는 축이 없음 |
| 자연스러운 군집 수 | **2개** | 5개가 아니다 |

> **설명변수가 돌봄부담을 5구간으로 가를 만큼의 정보를 담고 있지 않다.**
> 알고리즘을 바꿔도 크게 오르지 않는다.

같은 모델로 문제만 "고부담이냐 아니냐"로 바꾸면 **정확도 0.478 → 0.696** 으로 오른다.
데이터가 자연스럽게 나뉘는 수(2)와 서비스가 실제로 필요한 판정이 일치한다.

### ② "왜 최신 알고리즘을 안 썼나" — 붙여봤더니 졌다

| 알고리즘 | macro F1 | |
|---|---|---|
| **다항 로지스틱 회귀** | **0.318** | 채택 |
| 랜덤 포레스트 | 0.289 | |
| 엑스트라 트리 | 0.267 | |

**단순한 방법이 이겼다.** 학습 데이터가 2,398건뿐이라 복잡한 모델은 외워버리고,
①에서 확인했듯 애초에 복잡한 구조가 없었다.

### ③ 성능을 한 문장으로

> 다섯 구간을 정확히 집어내지는 못하지만(**정확도 47.8%**), **"지원이 필요한 사람인가"는
> 69.6% 정확도**로 가려낸다. 실제 고부담 보호자의 **77.3%** 를 잡아내고,
> 부담 큰 사람을 "부담 없음"으로 판정하는 **위험한 실수는 test 602건 중 0건**이다.

---

## 2. 모델 검증 — 전수 확인

### 학습·평가 절차 확인

- 분할이 **Stratified K-Fold** 임을 폴드별 구성으로 검증 (폴드 간 편차 **0.20%p**)
- 층화가 필요했던 이유 규명 — `부담없음` 이 61건(2.5%)뿐이라 무작위 분할이면 폴드마다
  8~17건으로 흔들리고 macro F1 이 튄다
- 분할이 코드가 아니라 **DB 컬럼(`split`·`cv_fold`)에 고정**돼 있음을 확인 (재현성)

### 혼동행렬

| | 교차검증 | test |
|---|---|---|
| 정확도 | 0.4629 | **0.4784** |
| 고부담 재현율 | 0.7843 | **0.7730** |
| 고부담 정밀도 | 0.6934 | **0.6981** |
| 폴드 간 편차 | 0.044 | — |

**test 가 교차검증보다 높다** — 과적합이 심하지 않다는 건강한 신호다.

읽어낸 것 네 가지:
- 답이 "고부담군"으로 몰린다 → 결정 가중치 1.1 의 의도된 효과
- 최고부담군을 한 칸 내려 판정하는 경우가 많다 → 둘 다 즉시 안내 대상이라 실질 피해 작음
- `부담없음`(61건)은 거의 못 맞힌다 → 서비스 개입 대상이 아니라 영향 작음
- **정반대 오류는 없다** → 가장 위험한 실수가 일어나지 않음

---

## 3. 문항 수 실험 — 7 vs 14

7문항이 적어 보인다는 우려에서 출발해 **14문항 모델을 실제로 만들어 비교**했다.

| | 7문항 `v1.0.0` | 14문항 `v1.1.0` |
|---|---|---|
| test macro F1 | 0.3416 | 0.3420 |
| test 고부담 재현율 | **0.7730** | 0.7669 |
| **test 판정 불가** | **2.49%** | **4.20%** |

**성능은 같은데 판정 불가만 1.7배 늘었다.** 문항을 두 배로 늘려 응답 부담과
결과를 못 받는 사람만 늘리고 정확도는 얻지 못했다 → **7문항 유지로 결정.**

원인도 규명했다 — 14문항에 들어온 **나이 변수 2개**가 희소성 점수를 끌어내렸다
(`caregiver_age` 78레벨, 평균 log p −3.782 / 나머지 문항은 −0.47~−1.59).

**후속 실험** — 나이를 구간화하면 해결되는지 시험했다. 희소성 지표는 개선됐지만
(평균 rarity −1.466 → −1.239, 원-핫 224 → 100열) **판정 불가 비율은 그대로**였다.
보정 단계가 임계값을 자동으로 맞추기 때문이며, **3% 는 데이터가 아니라 규칙이 겨냥한
목표값**이라는 사실을 알아냈다. 재현율은 오히려 떨어져 채택하지 않았다.

---

## 4. 판정 불가 규명 — 가설 4개 중 3개 기각

"판정 불가가 너무 자주 뜬다"에서 출발해 원인을 하나씩 확인했다.

| 가설 | 검증 방법 | 결과 |
|---|---|---|
| 확률 보정이 안 돼서 | 신뢰도 곡선·ECE | **기각** — ECE 0.042 로 이미 잘 보정됨 |
| margin 기준이 더 정확 | 같은 인원으로 통일해 비교 | **기각** — 현재 max 기준이 나음 (0.611 vs 0.569) |
| 문항 수 때문 | 나이 구간화 재학습 | **기각** — 임계값이 자동 보정됨 |
| **배포 설정이 틀림** | DB 설정 vs 모델 산출물 대조 | **적중** → 5장 |

**5~10분짜리 확인으로 반나절씩 걸릴 구현 세 건을 아꼈다.**

### 판정 불가 48명의 정체

- 1등·2등 확률 격차 **0.021** (정상 판정은 0.191) — 사실상 동점
- 응답이 "긍정적" 극단에 몰려 있는데(일과 매우 좋아함 41.7% vs 전체 9.3%)
  **실제로는 46%가 고부담군** — 응답과 실제가 어긋나는 사람들
- 억지로 답을 주면 **27%만 맞는다** (전체 46%)

> **판정 불가는 결함이 아니라 데이터의 한계를 정직하게 드러낸 결과다.**

### 화면 설계 (미구현)

"가까운 구간이라도 알려주자"는 안을 검토했으나 **실측으로 기각**했다 —
이 집단의 2단계 정확도가 **0.542** 인데 아무에게나 "큰 쪽"이라 답하기가 **0.543** 이다.
모델이 가진 정보가 없다.

대신 **추측이 아닌 사실만** 알려주는 두 유형 화면을 설계했다.

| 유형 | 인원 | 알려줄 사실 |
|---|---|---|
| A 드문 응답 | 24명(1%) | 어느 답이 조사 자료의 몇 % 인지 + "응답이 잘못된 것이 아닙니다" |
| B 확신 부족 | 48명(2%) | 답한 내용 요약 (상담 갈 때 그대로 활용) |

제목을 "판단할 수 없습니다"에서 **"이번에는 상담을 권해드립니다"** 로 바꿔
실패 통지가 아닌 대안 제시로 만든다. **2026-09-02 에 이어서 진행.**

---

## 5. 서비스 버그 발견·수정 🔴

**학습으로 계산한 값이 웹사이트에 전달되지 않고 있었다.**

| 설정 | 배포되던 값 | 실제 산출값 |
|---|---|---|
| `tauConf` 확신도 기준 | 0.34 (자리표시) | **0.3239** |
| `tauDens` 드문 조합 기준 | −9.0 → **조건이 아예 꺼짐** | **−2.4343** |
| `minThreshold` 기여 요인 강약 | 0.01 → **구분 무의미** | **0.1325** |

DB 설정은 11:42, 모델 학습은 11:51 — **설정이 먼저 들어가고 학습 결과가 반영되지 않았다.**
`tauDens −9.0` 은 실제 rarity 최솟값(−2.749)보다 훨씬 낮아 **희소성 판정이 한 번도
발동하지 않았다.** 스펙이 설계한 두 축 중 하나가 죽어 있었다.

**영향** — 판정 불가 83명(3.46%) → 72명(3.00%), 기여 요인 강약 구분 복구.

**근본 원인은 사람 실수가 아니라 작업 정의의 구멍이었다.** `T015`(설정 시드)가
모델보다 먼저 도는 구조인데 학습 후 재실행하라는 지시가 없었다.

**조치**
- `db/seeds/config.js` — 숫자를 손으로 적는 대신 `models/*.json` 에서 **직접 읽도록** 변경.
  재학습하고 시드만 돌리면 값이 자동으로 따라온다
- `tasks.md` — **T029a 신설** (설정 반영 + 백엔드 재기동). 의존 그래프와
  "실수하기 쉬운 지점"에도 사례로 기록

---

## 6. 보안 — 자격증명 분리

노트북 CONFIG 셀에 DB 비밀번호가 평문으로 박혀 있었다. 노트북은 팀원·심사자에게
전달되는 파일이라 전달할 때마다 함께 나간다.

- `.env` / `.env.example` / `.gitignore` 체계 도입
- 외부 패키지 없이 `.env` 를 읽는 로더 작성 (`python-dotenv` 의존 회피)
- 값이 없으면 어느 키가 빠졌는지 알려주고 멈추도록

---

## 7. 산출물

### 문서

| 파일 | 내용 |
|---|---|
| `docs/HG-다항로지스틱회귀-쉽게.md` | **발표용.** 실제 계수로 한 사람의 판정 과정을 4단계로 추적 |
| `docs/HG-모델-성능-리포트.md` | 혼동행렬·교차검증·성공 기준·한계 5가지 |
| `docs/HG-문항별-답변-영향표.md` / `.csv` | 53개 선택지별 계수와 "고부담쪽 밀기" |
| `docs/모델-학습-방법.md` | 모델·분할·파이프라인 (v1.0.0 기준) |
| `docs/실험기록-문항수와-판정불가.md` | 실험 2건과 판단 착오 기록 |

### 실행 노트북 (전부 실행 완료, 결과 포함)

| 파일 | 내용 |
|---|---|
| `notebooks/HG-EDA-7문항.ipynb` | 7문항 분포·상관관계(Cramér's V)·설명력 |
| `notebooks/HG-모델-성능-리포트.ipynb` | 층화 검증·교차검증·혼동행렬 히트맵 |
| `notebooks/HG-실험-군집화.ipynb` | **y 없이 군집화 — 발표 핵심 근거** |
| `notebooks/모델-학습-과정.ipynb` | 계열 비교·후진 제거 33단계·판정 불가 경계 |

노트북은 `ml/train.py` 의 함수를 그대로 불러 쓴다 — 계산을 두 벌 두면 나중에 갈라진다.

---

## 8. EDA 에서 나온 문항 관련 발견

| 발견 | 수치 |
|---|---|
| 문항 4(가구주)와 6(관계)이 강하게 연관 | Cramér's V **0.605**. 단 답이 같은 건 42%뿐이라 중복은 아님 |
| **문항 5(퇴사 이유)가 사실상 무력** | target 연관 **0.035**(7문항 중 꼴찌), 결측 88%, 선택지 16개 |
| 문항 5를 빼면 | 재현율 −0.008 로 거의 무해 → **팀 판단으로 유지 결정** |
| 문항 5의 계수가 소수 표본에 휘둘림 | "가족 돌봄이 필요해서" **6명**으로 계수 +1.345 |
| 선택지 이름표 미검증 | 문항 5의 15개 이름표가 공식 매핑표에 없음. 코드북 PDF 대조 필요 |

---

## 귀속 근거 — 읽는 사람을 위한 주의

- 이 문서의 내용은 **오현근 노트북 세션의 대화 이력**에 근거한다.
  `git log --author` 로는 구분할 수 없다 (전 커밋 작성자가 `pioneer3` 하나)
- **7문항 모델 `v1.0.0` 자체의 학습·배포는 조성기의 작업**이다.
  본인은 그 모델을 **검증·분석하고 배포 설정의 결함을 찾아 고쳤다**
- 홈페이지 디자인·지도 교체·발표자료는 다른 세션 작업이며 여기 포함하지 않는다

### 확인 필요 사항 (팀장 보고)

- `cb_model_version_v1` 의 `v1.0.0` 기록이 **14문항 평가 결과로 덮였다.**
  본인이 `evaluate` 를 돌릴 때 `MODEL_VERSION` 환경변수를 주지 않아 발생했다.
  원래 값은 **F1 0.3416 · 재현율 0.7730 · 판정불가 2.49%** (11:51:58) 이며 복구 가능하다
- 5장의 버그 수정(`db/seeds/config.js`·`tasks.md`)이 **아직 커밋되지 않았다.**
  누가 `npm run seed:config` 를 다시 돌리면 원복된다

---

## 2026-09-01 마감 시점 상태

```
홈페이지     https://p3.sumzip.com   정상 (7문항 v1.0.0)
판정 불가     3.00% (수정 전 3.46%)
보관 모델     models/v1.0.0(가동) · v1.1.0(14문항) · v1.2.0(나이 구간화)
```

### 내일 이어서 할 것

1. **판정 불가 안내 화면** — 설계 완료. 문서화할지 구현할지 결정 (FR-009a 스펙 수정 필요)
2. 학습 추가 시도 — 정규화(L1/L2) 튜닝부터. 서열 누적 이진, 계층 2단계 모델 순

---

## 2일차 — 2026-09-02 (시각 기록 시작)

이 날부터 **요청이 들어온 시각을 함께 남긴다.** 1일차까지의 기록은 날짜 단위 그대로 둔다.
같은 폴더를 여러 세션이 공유하므로, 이 절은 **오현근 세션에서 직접 지시·확인한 것만** 담는다.

| 시각 (KST) | 요청 | 처리 | 결과 |
|---|---|---|---|
| 09:22 | 어제 끊긴 **2번(판정 불가 안내)** 작업 상태 확인 | 작업 트리 전수 확인 — 추론·서비스·설정·프론트·계약·테스트 6개 지점 대조 | 메모에는 "설계만 완료"로 남아 있었으나 **구현이 이미 들어와 있었음.** `tsc` 통과·백엔드 63건·프론트 14건 전부 통과. 끊긴 지점은 **구현 직후, 스펙 반영과 커밋 직전** |
| 09:27 | 근거 목록(응답 요약·비율)은 **판정 불가일 때만** 내는 게 맞지 않나 | `diagnosisService` 갈림길과 `ResultPage` 분기 대조 | **이미 그렇게 돼 있음.** 같은 불리언 하나로 두 경로가 배타적. 다만 **이를 지키는 테스트가 없음**을 발견 |
| 09:30 | 쉽게 설명해 달라 | 판정 불가 두 유형과 화면 설계를 비전문 용어로 정리 | — |
| 09:33 | 회귀 테스트 추가 승인 | `backend/tests/unit/diagnosisBranches.test.ts` 신규 15건 | **78/78 통과**(63→78). 변이 시험 2회로 실제 검출력 확인 |
| 09:37 | **이 세션 사용자는 오현근**임을 밝힘 (팀 단위 작업이라 표시 필요) | 이 절 신설, 세션 귀속 메모 정정 | 2026-09-02 이 세션의 작업은 오현근 기여로 기록. 접두어 `HG-` 유지 |
| 09:41 | 모델이 바뀌었나 — 어제와 너무 다르다, 판정 불가가 안 뜨고 대부분 고부담군 | 산출물·코드·설정 전 경로 추적 | **모델 무변경 확인**(v1.0.0, md5가 아카이브와 동일, 09-01 15:10 이후 손댄 적 없음). 관측 현상은 정상값 — 판정 불가 2.0%, 고부담 60%. **조사 중 별개 결함 발견: 런타임이 DB 임계값을 안 읽음(FR-009c 위반)** |
| 09:48 | 고칠 수 있나, 아니면 오늘 작업을 되돌릴 수 있나 (실행 말고 답변만) | 되돌림 영향 분석 | 오늘 작업은 판정 로직을 건드리지 않아 **되돌려도 결과가 같음**. 조정 손잡이 2개(가산점·임계값)와 각각의 대가를 제시 |
| 09:52 | **5번 결함을 고치고**, 제대로 작동하는지 확인해 달라 | TDD로 수정 + 운영 DB 대상 읽기 전용 검증 | 임계값을 설정에서 읽도록 수정, 어긋남 감지·기동 로그 추가. **87/87 통과.** 운영 DB 확인 결과 **설정과 산출물이 이미 일치** — 이 수정으로 판정은 달라지지 않음 |
| 10:02 | 어제는 17:30까지 계속 작업했다 | 17:30 이후 바뀐 것을 시각순으로 추적 | **원인 확정 — 배포 불일치.** 프론트는 오늘 코드, 백엔드는 어제 17:12 프로세스 그대로였다 |
| 10:05 | 백엔드를 재기동해 달라 | 구 프로세스 종료 후 동일 방식으로 재기동, 운영 API로 3갈래 검증 | **10:06:24 재기동 완료.** 판정 불가 화면 복구. DB 쓰기 없이 검증(`consentTraining` 미전달) |
| 10:16 | **오늘 작업한 것을 어제 상태로 되돌려 달라** | 백업 후 오늘 변경분만 되돌림, DB·백엔드까지 정합 | 어제 작업(프론트 5건)은 그대로 두고 오늘 것만 제거. 시드 재실행·백엔드 재기동으로 코드·DB·프로세스 일치. 44/44 통과 |

### 오늘 만든 것 — 판정 불가 갈림길 회귀 테스트

`diagnose()` 를 통째로 돌려 **판정 / 판정 불가 두 갈래가 섞이지 않는지** 잠근다.

| 갈래 | 확인 |
|---|---|
| 판정된 응답 | 부담 구간·기여 요인·비교가 나오고 **`undecidable` 이 null** |
| 판정 불가 (양쪽 유형) | 부담 구간·기여 요인·비교·2인 보정 안내가 전부 null, 근거 목록은 여기서만 |
| 유형별 차이 | `sparse` 는 비율이 숫자, `ambiguous` 는 전부 null + 문항 수만큼 순서대로 |
| 문구 누수 | 안내문 JSON 안에 "최고부담군" 등 **구간 명칭이 한 글자도 없음** |

입력은 손으로 박지 않고 `models/parity_v1.json` 3,000건에서 갈래별 실제 응답을 골라 쓴다.
값을 고정하면 재학습 후 그 응답이 다른 갈래로 옮겨가 엉뚱한 이유로 깨진다.

**검출력을 실제로 확인했다** — 통과만으로는 울타리 역할의 증거가 되지 않아 코드를 일부러 망가뜨렸다.

| 변이 | 잡혔나 |
|---|---|
| 판정돼도 안내문을 조립하도록 변경 | ✅ `★ 근거 목록을 내지 않는다` 실패 |
| 판정 불가에도 기여 요인을 계산하도록 변경 | ✅ 양쪽 유형 `기여 요인과 비교를 내지 않는다` 실패 |

원본과 `diff` 로 차이 없음을 확인해 되돌렸다.

### 5번 결함 수정 — 판정 불가 임계값을 설정에서 읽는다 (FR-009c)

`diagnosisService` 가 임계값을 `models/uncertainty_v1.json` 에서 직접 읽고 있었다. DB의
`undecidable.thresholds` 는 기동 시 존재만 확인하고 **판정에 쓰이지 않았다.** 스펙이 요구하는
"코드 수정 없이 조정 가능"(FR-009c)이 성립하지 않는 상태였고, 09-01 에 고친 시드 값도
서비스 동작을 바꾸지 못했다.

| 바뀐 것 | 내용 |
|---|---|
| `diagnosisService.ts` | `cfg('undecidable.thresholds')` 를 읽어 판정한다 |
| `inference/undecidable.ts` | `thresholdDrift()` — 설정과 산출물이 어긋나면 문구를 낸다 |
| `server.ts` | 기동 로그에 **실제 적용 중인 임계값**을 찍고, 어긋나면 경고한다 |
| 테스트 | `undecidableThresholds.test.ts` 4건 + 어긋남 감지 5건 (87/87) |

설정이 이기게 둔 이유 — 어긋남을 기동 실패로 처리하면 설정만으로 조정할 수 없게 되어
FR-009c 와 충돌한다. 그래서 **판정은 설정을 따르고, 어긋남은 경고로 드러낸다.**

### 운영 DB 대상 검증 (읽기 전용, 09:56)

```
1) 설정 로드            OK — 필수 키 전부 존재
2) 아티팩트 정합 검증    OK — v1.0.0 (logit, 7문항), 문항집합 qs-v1.0.0, DB 문항 집합 일치
3) 적용 중인 임계값      설정 0.32391675903458894 / -2.4342723737738483
                       산출물 0.32391675903458894 / -2.4342723737738483  → 일치
4) 실제 진단 3건        판정 성립 → 중간부담군·기여요인 3·비교 4·근거목록 0
                       드문 응답 → sparse·근거목록 1·연계 있음
                       확신 부족 → ambiguous·근거목록 7·연계 있음
5) 3,000건 분포         판정 불가 60건(2.00%) = sparse 32 + ambiguous 28
                       고부담(1+2) 60.0% · 중간 27.9% · 저 9.9% · 없음 0.2%
```

**시드는 이미 재실행돼 있었다**(T029a 완료). 설정과 산출물이 일치하므로 이번 수정으로
판정 결과는 달라지지 않는다. 규정 위반만 해소되고 가시성이 생겼다.

### 원인 규명 — "어제와 다르다" 는 모델이 아니라 배포 불일치였다

모델은 바뀐 적이 없다(v1.0.0, 09-01 15:10 이후 무변경, 아카이브와 md5 동일).
갈린 지점은 **구성요소마다 서비스 중인 코드의 시점이 달랐던 것**이다.

| 구성요소 | 어제 17:30 | 오늘 10:00 | 왜 |
|---|---|---|---|
| 프론트 (:9503) | 어제 코드 | **오늘 09:13 코드** | `vite dev` 로 떠 있어 소스 수정이 즉시 반영된다 |
| 백엔드 (:9523) | 어제 코드 | **어제 17:12 코드** | `npm exec tsx src/server.ts` — **watch 모드가 아니라** 재기동 전까지 그대로 |
| DB 설정 | 어제 값 | **오늘 09:11 시드** | 문구가 두 유형 구조로 교체됨 |

**증상의 메커니즘** — 새 프론트는 응답의 `undecidable` 객체를 찾는데 옛 백엔드는 `undecidableNotice`
문자열만 보냈다. `ResultPage` 는 `v-if="r.decided"` / `v-else-if="und"` 로 갈리므로 판정 불가인
사람은 **둘 다 false 가 되어 아무 카드도 그려지지 않았다.** "판정 불가가 안 뜬다" 의 정체다.
"대부분 고부담군" 은 이와 무관한 정상값(60.0%)이고, 두 현상이 겹쳐 보였다.

**교훈** — 프론트가 빌드 산출물이 아니라 개발 서버(`vite dev`)로 운영되고 있어, 소스만 고쳐도
즉시 배포된다. 백엔드는 수동 재기동이 필요하다. **이 비대칭이 오늘 어긋남의 구조적 원인이다.**
팀장에게 알릴 것.

### 백엔드 재기동과 검증 (10:06)

```
[cb-backend] http://localhost:9523/api/v1
  모델        : v1.0.0 (logit, 문항 7개)
  문항 집합   : qs-v1.0.0
  판정 불가   : 확신도 < 0.32391675903458894 · 희소성 < -2.4342723737738483 (설정값)
                                                        ← 어긋남 경고 없음
```

운영 API 로 세 갈래 확인 (`consentTraining` 을 주지 않아 **DB 에 아무 행도 만들지 않음**, FR-031).

| 응답 | 결과 |
|---|---|
| 판정 성립 | decided=true · 중간부담군 · 기여요인 3 · 비교 4 · `undecidable` null |
| 드문 응답 | decided=false · sparse · "이번에는 상담을 권해드립니다" · 근거 1 · 연계 있음 |
| 확신 부족 | decided=false · ambiguous · 근거 7 · 연계 있음 |

구식 `undecidableNotice` 필드는 응답에서 사라졌고, 프론트가 기대하는 계약과 일치한다.

### 남은 것

1. **스펙 FR-009a 개정** — 판정 불가를 희소성 하나로만 정의하고 있어 `ambiguous`(확신 부족)가
   스펙에 없다. 구현은 두 유형인데 스펙은 한 유형이다. **팀 합의 필요**
2. ~~시드 재실행(T029a)~~ — **완료 확인.** DB 의 `undecidable.thresholds` 는 학습 산출값과 일치하고
   `undecidable.noticeText` 도 두 유형 구조로 들어가 있다 (오늘 09:11 시드 반영)
3. 프론트 `result.test.ts` 에 판정 불가 두 유형 렌더 커버리지 없음
4. 테스트 설정 문구가 `db/seeds/config.js` 값을 옮겨 적은 것이라 **시드가 바뀌어도 안 깨진다.**
   시드에서 `CONFIG` 를 export 로 분리하면 그 드리프트까지 잡히나, 팀 공용 파일 구조 변경이라 보류

---

## 되돌림 (10:19) — 이 절의 코드 작업은 현재 저장소에 없다

10:16 요청으로 **오늘 작업분을 전부 되돌렸다.** 위 기록은 무엇을 했고 왜 했는지의 이력으로 남긴다.

**되돌린 것 (오늘 09:11~09:55 변경분)**

| 종류 | 파일 |
|---|---|
| 되돌림 | `db/seeds/config.js` · `openapi.yaml` · `ResultPage.vue` · `diagnosisService.ts` · `undecidable.ts` · `server.ts` |
| 삭제 | `undecidableNotice.ts` · 테스트 4종(`undecidableNotice` · `undecidableReason` · `diagnosisBranches` · `undecidableThresholds`) |

**그대로 둔 것** — 어제 작업분(`PreSurveyPage.vue` · `App.vue` · `HomePage.vue` · `theme-claude.css` ·
`presurvey.test.ts`)과 오늘 09:06 커밋 2건(`7ac5a7d` 설정 출처 수정 · `aebcbd0` 문서). 이 둘은
**어제 작업이 오늘 아침 커밋된 것**이라 되돌리지 않았다.

**코드만 되돌리면 다시 어긋나므로 세 곳을 함께 맞췄다.**

1. 코드 → 어제 상태
2. `npm run seed:config` 재실행 → DB 문구가 옛 단일 문자열로 복원
3. 백엔드 재기동 → 되돌린 코드로 구동

검증: `판정 불가 → undecidableNotice` 문자열 정상 반환, 판정 성립 → null. 프론트(vite dev)도
되돌린 `ResultPage.vue` 를 서비스 중. 테스트 44/44 · `tsc` 통과.

**백업** — 되돌린 작업은 전부 복구 가능하다.

```
scratchpad/오늘작업-백업-1002/
  오늘-추적파일.patch          git apply 로 복원
  untracked/                   삭제한 신규 파일 5개 원본
```

되살리려면 패치를 적용하고 `untracked/` 를 제자리에 복사한 뒤, 시드 재실행과 백엔드
재기동을 다시 해야 한다(세 곳을 함께 맞춰야 한다는 점은 동일).

---

## 10:2x~ — 분석용 joblib 과 v1.0.0 전수 분석

| 시각 (KST) | 요청 | 처리 | 결과 |
|---|---|---|---|
| 10:22 | 모델 아티팩트 위치 · joblib 형태는 없는지 | `ml/train.py` 저장 방식 확인 | JSON 만 저장한다. 서버가 Node 라 joblib 을 못 읽어 **계수만 내보내고 TS 가 직접 계산**하는 구조. 대신 parity 3,000건이 두 구현 일치를 보증 |
| 10:3x | **분석용 joblib 생성** + v1.0.0 모델·데이터 분석 문서 | 재학습 없이 JSON 에서 복원 · 운영 DB 읽기 전용 분석 | `models/model_v1.joblib` (오차 3.3e-16) · `docs/HG_모델-데이터-분석-v1.0.0.md` |

### 만든 것

| 파일 | 역할 |
|---|---|
| `models/model_v1.joblib` · `models/v1.0.0/model_v1.joblib` | 분석 전용 sklearn 객체. **배포 대상 아님** |
| `ml/HG_rebuild_joblib.py` | 배포본을 덮지 않고 JSON 에서 복원. parity 3,000건으로 검증하고 불일치면 파일을 만들지 않는다 |
| `ml/HG_analyze_v1.py` | 문서의 모든 수치를 만드는 분석 스크립트 (운영 DB 읽기 전용) |
| `ml/train.py` (수정) | export 단계에서 앞으로는 학습 직후 joblib 을 함께 남긴다 |
| `docs/HG_모델-데이터-분석-v1.0.0.md` | 배포 판단용 전수 분석 |

**재학습하지 않은 이유** — `train.py --stage export` 는 `models/*.json` 을 새로 쓴다. v1.0.0 이
운영 중이라 덮을 수 없어, JSON 의 계수·범주에서 sklearn 객체를 복원하고 parity 3,000건으로
동일성을 확인했다(최대 오차 3.3e-16).

### 분석 결론 (test 602건)

```
macro F1 0.3470 · 5구간 정확도 0.4872
고부담(1~2) 재현율 0.7736 · 이진 정확도 0.6985      ← SC-005 통과
판정 불가 2.49% · 판정불가군 오분류 0.8667 > 판정군 0.5128   ← SC-016 통과
확률 보정 ECE test 0.0268                          ← 보정 불필요
```

**배포 가능**하되 발표에서는 "5구간을 맞히는 서비스"가 아니라 **"고부담을 놓치지 않는 서비스"**
로 말해야 한다. 오차가 인접 구간에 몰려 있어(실제 1번을 4·5번으로 본 것은 89건 중 1건) 안내
목적에서 위험한 방향으로 틀리지 않는다.

### 문서에 적어 둔 배포 전 위험 3가지

1. **DB `cb_model_version_v1` 의 v1.0.0 행이 14문항 평가로 오염** — 판정불가율 0.0415(실제 0.0249).
   발표에 이 표를 쓰면 틀린 숫자가 나간다
2. **프론트가 `vite dev` 로 운영** — 소스만 고쳐도 즉시 배포되는데 백엔드는 수동 재기동이라,
   오늘 오전 사고의 구조적 원인이다
3. **판정 불가 임계값을 런타임이 설정에서 읽지 않음**(FR-009c). 지금은 두 값이 같아 동작 문제는 없다

### 바로잡은 인용 하나

어제 기록의 `ECE 0.042` 는 문서에서 근거를 찾지 못해 직접 계산해 넣었다 —
**train 0.0197 / test 0.0268**(10구간 가중평균, 결정 가중치를 뺀 순수 argmax 기준).
결론(보정 불필요)은 같다.

---

## 11:0x~ — 학습·추론 라이브러리 설계와 구현 계획

| 시각 (KST) | 요청 | 처리 | 결과 |
|---|---|---|---|
| 10:50 | 학습 과정을 라이브러리로 만들 계획 | 브레인스토밍 — 경계·범용성·데이터 입력 3가지 결정 | 학습 파이프라인만 · 돌봄부담 전용 · 스냅샷 고정 |
| 11:1x | 설계 승인, md 로 저장 | `docs/HG_학습-라이브러리-설계.md` | 자체 검토에서 `train` 이 test 를 매번 소모하는 문제를 잡아 `evaluate` 를 분리 |
| 11:3x | 14문항 DB 기록 문제와 joblib 서비스 방식 질의 | 답변 후 방향 결정 | **B안으로 DB 정정 완료** · **파이썬 추론 서비스로 전환 결정** |
| 11:4x | 설계 개정 + 구현 계획 + 5회 검토 | 설계 v2 · 계획 19태스크 · 검토기록 | 아래 참조 |

### 만든 것

| 문서 | 내용 |
|---|---|
| `docs/HG_학습-라이브러리-설계.md` | v2 — 범위를 추론 서비스까지 확장 |
| `docs/HG_학습-라이브러리-구현계획.md` | Phase A 11태스크 + Phase B 8태스크, 전부 TDD |
| `docs/HG_설계-검토기록.md` | 5회 검토에서 발견·조치한 것 |

### 방향 전환 — 파이썬 추론 서비스

지금은 파이썬이 계수를 JSON 으로 뽑고 TypeScript 가 추론을 다시 구현한다. 선형 모델이라
성립하지만 **모델 계열을 바꾸면 이식 비용이 급격히 커진다.** 앞으로 모델을 바꿀 수 있다는
판단에 따라 학습한 객체(joblib)를 서비스가 그대로 쓰는 구조로 간다.

```
[Vue :9503] ── [Node :9523] ──HTTP──▶ [파이썬 추론 :9533]
```

**핵심 판단** — 판정 정책(임계값·가중치)은 Node 가 요청에 실어 보낸다. 파이썬은 상태 없는
계산기가 되고, FR-009c(설정으로 조정)가 유지되며, 설정을 바꿔도 파이썬 재기동이 필요 없다.

**장애 처리** — 파이썬이 죽으면 "지금은 진단을 드릴 수 없습니다" 로 정직하게 알리되
**기관 안내는 계속 제공**한다. 판정 불가와 같은 안전망 경로를 재사용한다.

**되돌리기** — B4 까지는 `CB_INFERENCE_URL` 로 즉시 복구할 수 있다. TS 추론 제거(B7)는
별도 승인을 받는다.

### 5회 검토에서 잡은 것

- **신설 태스크 3개** — 골든 생성(설계가 요구하는데 만드는 곳이 없었다) · 전환 전 TS↔파이썬
  3,000건 대조 · 문서와 운영 절차
- **깨질 코드 2곳** — `contributionThreshold` 출처, `modelVersion` 타입
- **새 위험 1건** — 결정 가중치를 설정으로 옮기면 산출물과 갈라질 수 있다 → `/health` 대조 경고
- **환경 문제 2건** — `pytest` 미설치, `fastapi` 가 `.pylibs` 가 아닌 사용자 계정에만 있음
- **자리표시 1건** — 프론트 테스트를 컴포넌트 단위로 구체화

### 14문항 DB 기록 정정 (11:37)

`cb_model_version_v1` 의 v1.0.0 행이 14문항 평가 결과로 덮여 있던 것을 정정했다.
값을 손으로 고치지 않고 `MODEL_VERSION=v1.0.0 python3 ml/train.py evaluate` 로 같은 코드
경로에서 다시 만들었다.

```
정정 전  F1 0.3420 · 재현율 0.7669 · 판정불가 0.0415   ← 14문항 결과
정정 후  F1 0.3416 · 재현율 0.7730 · 판정불가 0.0249   ← 7문항 v1.0.0
```

정정 전 값은 `scratchpad/cb_model_version_v1-정정전-백업.json` 에 보존했다.
14문항 평가 기록은 이 정정으로 사라졌다 — 남기려면 모델을 바꿔 끼우고 평가해야 하므로
배포 중에는 하지 않았다.
