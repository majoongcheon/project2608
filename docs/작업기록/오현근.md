# 작업 기록 — 오현근

**기간**: 2026-09-01 (1일차) · 2026-09-02 (2일차)
**근거**: 이 세션(노트북)에서 직접 지시하고 확인한 작업. 아래 "귀속 근거" 참조.
**산출물 접두어**: `HG-` (현근)

---

## 한눈에

| 영역 | 한 일 | 결과 |
|---|---|---|
| **모델 검증** | 학습·교차검증·test 성능을 혼동행렬까지 전수 확인 | 성능 리포트 확정 |
| **문항 수 실험** | 7 → 14문항으로 늘려 실측 비교 | **7문항 유지 결정** |
| **한계 규명** | y 없이 군집화해 데이터 구조를 확인 | **"정확도 48%는 모델 탓이 아님" 입증** |
| **판정 불가 규명** | 왜 뜨는지, 줄일 수 있는지 4가지 가설 검증 | 3개 기각, 원인 확정 |
| **서비스 버그** | 학습값이 배포에 반영 안 되던 문제 발견·수정 | **판정 불가 83→72명, 꺼져 있던 조건 복구** |
| **보안** | 노트북에 박힌 DB 비밀번호 분리 | `.env` 체계 도입 |
| **문서·발표 자료** | 설명 문서 3종, 실행 노트북 4종 | 발표 근거 확보 |

---

## 1. 발표에 바로 쓸 수 있는 세 가지

### ① "왜 정확도가 48%인가" — 데이터로 답했다

가장 나올 만한 질문이다. **정답을 가리고 군집화**해서 답을 만들었다.

| 지표 | 실측 | 뜻 |
|---|---|---|
| 실루엣 계수 | **0.12** | 0.25 미만 = 뚜렷한 덩어리 구조 없음 |
| 부담 구간과 일치도 (ARI) | **0.027** | 0에 붙음 = 거의 무관 |
| PCA 1번 성분 | **12.9%** | 데이터를 관통하는 축이 없음 |
| 자연스러운 군집 수 | **2개** | 5개가 아니다 |

> **설명변수가 돌봄부담을 5구간으로 가를 만큼의 정보를 담고 있지 않다.**
> 알고리즘을 바꿔도 크게 오르지 않는다.

같은 모델로 문제만 "고부담이냐 아니냐"로 바꾸면 **정확도 0.478 → 0.696** 으로 오른다.
데이터가 자연스럽게 나뉘는 수(2)와 서비스가 실제로 필요한 판정이 일치한다.

### ② "왜 최신 알고리즘을 안 썼나" — 붙여봤더니 졌다

| 알고리즘 | macro F1 | |
|---|---|---|
| **다항 로지스틱 회귀** | **0.318** | 채택 |
| 랜덤 포레스트 | 0.289 | |
| 엑스트라 트리 | 0.267 | |

**단순한 방법이 이겼다.** 학습 데이터가 2,398건뿐이라 복잡한 모델은 외워버리고,
①에서 확인했듯 애초에 복잡한 구조가 없었다.

### ③ 성능을 한 문장으로

> 다섯 구간을 정확히 집어내지는 못하지만(**정확도 47.8%**), **"지원이 필요한 사람인가"는
> 69.6% 정확도**로 가려낸다. 실제 고부담 보호자의 **77.3%** 를 잡아내고,
> 부담 큰 사람을 "부담 없음"으로 판정하는 **위험한 실수는 test 602건 중 0건**이다.

---

## 2. 모델 검증 — 전수 확인

### 학습·평가 절차 확인

- 분할이 **Stratified K-Fold** 임을 폴드별 구성으로 검증 (폴드 간 편차 **0.20%p**)
- 층화가 필요했던 이유 규명 — `부담없음` 이 61건(2.5%)뿐이라 무작위 분할이면 폴드마다
  8~17건으로 흔들리고 macro F1 이 튄다
- 분할이 코드가 아니라 **DB 컬럼(`split`·`cv_fold`)에 고정**돼 있음을 확인 (재현성)

### 혼동행렬

| | 교차검증 | test |
|---|---|---|
| 정확도 | 0.4629 | **0.4784** |
| 고부담 재현율 | 0.7843 | **0.7730** |
| 고부담 정밀도 | 0.6934 | **0.6981** |
| 폴드 간 편차 | 0.044 | — |

**test 가 교차검증보다 높다** — 과적합이 심하지 않다는 건강한 신호다.

읽어낸 것 네 가지:
- 답이 "고부담군"으로 몰린다 → 결정 가중치 1.1 의 의도된 효과
- 최고부담군을 한 칸 내려 판정하는 경우가 많다 → 둘 다 즉시 안내 대상이라 실질 피해 작음
- `부담없음`(61건)은 거의 못 맞힌다 → 서비스 개입 대상이 아니라 영향 작음
- **정반대 오류는 없다** → 가장 위험한 실수가 일어나지 않음

---

## 3. 문항 수 실험 — 7 vs 14

7문항이 적어 보인다는 우려에서 출발해 **14문항 모델을 실제로 만들어 비교**했다.

| | 7문항 `v1.0.0` | 14문항 `v1.1.0` |
|---|---|---|
| test macro F1 | 0.3416 | 0.3420 |
| test 고부담 재현율 | **0.7730** | 0.7669 |
| **test 판정 불가** | **2.49%** | **4.20%** |

**성능은 같은데 판정 불가만 1.7배 늘었다.** 문항을 두 배로 늘려 응답 부담과
결과를 못 받는 사람만 늘리고 정확도는 얻지 못했다 → **7문항 유지로 결정.**

원인도 규명했다 — 14문항에 들어온 **나이 변수 2개**가 희소성 점수를 끌어내렸다
(`caregiver_age` 78레벨, 평균 log p −3.782 / 나머지 문항은 −0.47~−1.59).

**후속 실험** — 나이를 구간화하면 해결되는지 시험했다. 희소성 지표는 개선됐지만
(평균 rarity −1.466 → −1.239, 원-핫 224 → 100열) **판정 불가 비율은 그대로**였다.
보정 단계가 임계값을 자동으로 맞추기 때문이며, **3% 는 데이터가 아니라 규칙이 겨냥한
목표값**이라는 사실을 알아냈다. 재현율은 오히려 떨어져 채택하지 않았다.

---

## 4. 판정 불가 규명 — 가설 4개 중 3개 기각

"판정 불가가 너무 자주 뜬다"에서 출발해 원인을 하나씩 확인했다.

| 가설 | 검증 방법 | 결과 |
|---|---|---|
| 확률 보정이 안 돼서 | 신뢰도 곡선·ECE | **기각** — ECE 0.042 로 이미 잘 보정됨 |
| margin 기준이 더 정확 | 같은 인원으로 통일해 비교 | **기각** — 현재 max 기준이 나음 (0.611 vs 0.569) |
| 문항 수 때문 | 나이 구간화 재학습 | **기각** — 임계값이 자동 보정됨 |
| **배포 설정이 틀림** | DB 설정 vs 모델 산출물 대조 | **적중** → 5장 |

**5~10분짜리 확인으로 반나절씩 걸릴 구현 세 건을 아꼈다.**

### 판정 불가 48명의 정체

- 1등·2등 확률 격차 **0.021** (정상 판정은 0.191) — 사실상 동점
- 응답이 "긍정적" 극단에 몰려 있는데(일과 매우 좋아함 41.7% vs 전체 9.3%)
  **실제로는 46%가 고부담군** — 응답과 실제가 어긋나는 사람들
- 억지로 답을 주면 **27%만 맞는다** (전체 46%)

> **판정 불가는 결함이 아니라 데이터의 한계를 정직하게 드러낸 결과다.**

### 화면 설계 (미구현)

"가까운 구간이라도 알려주자"는 안을 검토했으나 **실측으로 기각**했다 —
이 집단의 2단계 정확도가 **0.542** 인데 아무에게나 "큰 쪽"이라 답하기가 **0.543** 이다.
모델이 가진 정보가 없다.

대신 **추측이 아닌 사실만** 알려주는 두 유형 화면을 설계했다.

| 유형 | 인원 | 알려줄 사실 |
|---|---|---|
| A 드문 응답 | 24명(1%) | 어느 답이 조사 자료의 몇 % 인지 + "응답이 잘못된 것이 아닙니다" |
| B 확신 부족 | 48명(2%) | 답한 내용 요약 (상담 갈 때 그대로 활용) |

제목을 "판단할 수 없습니다"에서 **"이번에는 상담을 권해드립니다"** 로 바꿔
실패 통지가 아닌 대안 제시로 만든다. **2026-09-02 에 이어서 진행.**

---

## 5. 서비스 버그 발견·수정 🔴

**학습으로 계산한 값이 웹사이트에 전달되지 않고 있었다.**

| 설정 | 배포되던 값 | 실제 산출값 |
|---|---|---|
| `tauConf` 확신도 기준 | 0.34 (자리표시) | **0.3239** |
| `tauDens` 드문 조합 기준 | −9.0 → **조건이 아예 꺼짐** | **−2.4343** |
| `minThreshold` 기여 요인 강약 | 0.01 → **구분 무의미** | **0.1325** |

DB 설정은 11:42, 모델 학습은 11:51 — **설정이 먼저 들어가고 학습 결과가 반영되지 않았다.**
`tauDens −9.0` 은 실제 rarity 최솟값(−2.749)보다 훨씬 낮아 **희소성 판정이 한 번도
발동하지 않았다.** 스펙이 설계한 두 축 중 하나가 죽어 있었다.

**영향** — 판정 불가 83명(3.46%) → 72명(3.00%), 기여 요인 강약 구분 복구.

**근본 원인은 사람 실수가 아니라 작업 정의의 구멍이었다.** `T015`(설정 시드)가
모델보다 먼저 도는 구조인데 학습 후 재실행하라는 지시가 없었다.

**조치**
- `db/seeds/config.js` — 숫자를 손으로 적는 대신 `models/*.json` 에서 **직접 읽도록** 변경.
  재학습하고 시드만 돌리면 값이 자동으로 따라온다
- `tasks.md` — **T029a 신설** (설정 반영 + 백엔드 재기동). 의존 그래프와
  "실수하기 쉬운 지점"에도 사례로 기록

---

## 6. 보안 — 자격증명 분리

노트북 CONFIG 셀에 DB 비밀번호가 평문으로 박혀 있었다. 노트북은 팀원·심사자에게
전달되는 파일이라 전달할 때마다 함께 나간다.

- `.env` / `.env.example` / `.gitignore` 체계 도입
- 외부 패키지 없이 `.env` 를 읽는 로더 작성 (`python-dotenv` 의존 회피)
- 값이 없으면 어느 키가 빠졌는지 알려주고 멈추도록

---

## 7. 산출물

### 문서

| 파일 | 내용 |
|---|---|
| `docs/HG-다항로지스틱회귀-쉽게.md` | **발표용.** 실제 계수로 한 사람의 판정 과정을 4단계로 추적 |
| `docs/HG-모델-성능-리포트.md` | 혼동행렬·교차검증·성공 기준·한계 5가지 |
| `docs/HG-문항별-답변-영향표.md` / `.csv` | 53개 선택지별 계수와 "고부담쪽 밀기" |
| `docs/모델-학습-방법.md` | 모델·분할·파이프라인 (v1.0.0 기준) |
| `docs/실험기록-문항수와-판정불가.md` | 실험 2건과 판단 착오 기록 |

### 실행 노트북 (전부 실행 완료, 결과 포함)

| 파일 | 내용 |
|---|---|
| `notebooks/HG-EDA-7문항.ipynb` | 7문항 분포·상관관계(Cramér's V)·설명력 |
| `notebooks/HG-모델-성능-리포트.ipynb` | 층화 검증·교차검증·혼동행렬 히트맵 |
| `notebooks/HG-실험-군집화.ipynb` | **y 없이 군집화 — 발표 핵심 근거** |
| `notebooks/모델-학습-과정.ipynb` | 계열 비교·후진 제거 33단계·판정 불가 경계 |

노트북은 `ml/train.py` 의 함수를 그대로 불러 쓴다 — 계산을 두 벌 두면 나중에 갈라진다.

---

## 8. EDA 에서 나온 문항 관련 발견

| 발견 | 수치 |
|---|---|
| 문항 4(가구주)와 6(관계)이 강하게 연관 | Cramér's V **0.605**. 단 답이 같은 건 42%뿐이라 중복은 아님 |
| **문항 5(퇴사 이유)가 사실상 무력** | target 연관 **0.035**(7문항 중 꼴찌), 결측 88%, 선택지 16개 |
| 문항 5를 빼면 | 재현율 −0.008 로 거의 무해 → **팀 판단으로 유지 결정** |
| 문항 5의 계수가 소수 표본에 휘둘림 | "가족 돌봄이 필요해서" **6명**으로 계수 +1.345 |
| 선택지 이름표 미검증 | 문항 5의 15개 이름표가 공식 매핑표에 없음. 코드북 PDF 대조 필요 |

---

## 귀속 근거 — 읽는 사람을 위한 주의

- 이 문서의 내용은 **오현근 노트북 세션의 대화 이력**에 근거한다.
  `git log --author` 로는 구분할 수 없다 (전 커밋 작성자가 `pioneer3` 하나)
- **7문항 모델 `v1.0.0` 자체의 학습·배포는 조성기의 작업**이다.
  본인은 그 모델을 **검증·분석하고 배포 설정의 결함을 찾아 고쳤다**
- 홈페이지 디자인·지도 교체·발표자료는 다른 세션 작업이며 여기 포함하지 않는다

### 확인 필요 사항 (팀장 보고)

- `cb_model_version_v1` 의 `v1.0.0` 기록이 **14문항 평가 결과로 덮였다.**
  본인이 `evaluate` 를 돌릴 때 `MODEL_VERSION` 환경변수를 주지 않아 발생했다.
  원래 값은 **F1 0.3416 · 재현율 0.7730 · 판정불가 2.49%** (11:51:58) 이며 복구 가능하다
- 5장의 버그 수정(`db/seeds/config.js`·`tasks.md`)이 **아직 커밋되지 않았다.**
  누가 `npm run seed:config` 를 다시 돌리면 원복된다

---

## 2026-09-01 마감 시점 상태

```
홈페이지     https://p3.sumzip.com   정상 (7문항 v1.0.0)
판정 불가     3.00% (수정 전 3.46%)
보관 모델     models/v1.0.0(가동) · v1.1.0(14문항) · v1.2.0(나이 구간화)
```

### 내일 이어서 할 것

1. **판정 불가 안내 화면** — 설계 완료. 문서화할지 구현할지 결정 (FR-009a 스펙 수정 필요)
2. 학습 추가 시도 — 정규화(L1/L2) 튜닝부터. 서열 누적 이진, 계층 2단계 모델 순

---

## 2일차 — 2026-09-02 (시각 기록 시작)

이 날부터 **요청이 들어온 시각을 함께 남긴다.** 1일차까지의 기록은 날짜 단위 그대로 둔다.
같은 폴더를 여러 세션이 공유하므로, 이 절은 **오현근 세션에서 직접 지시·확인한 것만** 담는다.

| 시각 (KST) | 요청 | 처리 | 결과 |
|---|---|---|---|
| 09:22 | 어제 끊긴 **2번(판정 불가 안내)** 작업 상태 확인 | 작업 트리 전수 확인 — 추론·서비스·설정·프론트·계약·테스트 6개 지점 대조 | 메모에는 "설계만 완료"로 남아 있었으나 **구현이 이미 들어와 있었음.** `tsc` 통과·백엔드 63건·프론트 14건 전부 통과. 끊긴 지점은 **구현 직후, 스펙 반영과 커밋 직전** |
| 09:27 | 근거 목록(응답 요약·비율)은 **판정 불가일 때만** 내는 게 맞지 않나 | `diagnosisService` 갈림길과 `ResultPage` 분기 대조 | **이미 그렇게 돼 있음.** 같은 불리언 하나로 두 경로가 배타적. 다만 **이를 지키는 테스트가 없음**을 발견 |
| 09:30 | 쉽게 설명해 달라 | 판정 불가 두 유형과 화면 설계를 비전문 용어로 정리 | — |
| 09:33 | 회귀 테스트 추가 승인 | `backend/tests/unit/diagnosisBranches.test.ts` 신규 15건 | **78/78 통과**(63→78). 변이 시험 2회로 실제 검출력 확인 |
| 09:37 | **이 세션 사용자는 오현근**임을 밝힘 (팀 단위 작업이라 표시 필요) | 이 절 신설, 세션 귀속 메모 정정 | 2026-09-02 이 세션의 작업은 오현근 기여로 기록. 접두어 `HG-` 유지 |
| 09:41 | 모델이 바뀌었나 — 어제와 너무 다르다, 판정 불가가 안 뜨고 대부분 고부담군 | 산출물·코드·설정 전 경로 추적 | **모델 무변경 확인**(v1.0.0, md5가 아카이브와 동일, 09-01 15:10 이후 손댄 적 없음). 관측 현상은 정상값 — 판정 불가 2.0%, 고부담 60%. **조사 중 별개 결함 발견: 런타임이 DB 임계값을 안 읽음(FR-009c 위반)** |
| 09:48 | 고칠 수 있나, 아니면 오늘 작업을 되돌릴 수 있나 (실행 말고 답변만) | 되돌림 영향 분석 | 오늘 작업은 판정 로직을 건드리지 않아 **되돌려도 결과가 같음**. 조정 손잡이 2개(가산점·임계값)와 각각의 대가를 제시 |
| 09:52 | **5번 결함을 고치고**, 제대로 작동하는지 확인해 달라 | TDD로 수정 + 운영 DB 대상 읽기 전용 검증 | 임계값을 설정에서 읽도록 수정, 어긋남 감지·기동 로그 추가. **87/87 통과.** 운영 DB 확인 결과 **설정과 산출물이 이미 일치** — 이 수정으로 판정은 달라지지 않음 |
| 10:02 | 어제는 17:30까지 계속 작업했다 | 17:30 이후 바뀐 것을 시각순으로 추적 | **원인 확정 — 배포 불일치.** 프론트는 오늘 코드, 백엔드는 어제 17:12 프로세스 그대로였다 |
| 10:05 | 백엔드를 재기동해 달라 | 구 프로세스 종료 후 동일 방식으로 재기동, 운영 API로 3갈래 검증 | **10:06:24 재기동 완료.** 판정 불가 화면 복구. DB 쓰기 없이 검증(`consentTraining` 미전달) |
| 10:16 | **오늘 작업한 것을 어제 상태로 되돌려 달라** | 백업 후 오늘 변경분만 되돌림, DB·백엔드까지 정합 | 어제 작업(프론트 5건)은 그대로 두고 오늘 것만 제거. 시드 재실행·백엔드 재기동으로 코드·DB·프로세스 일치. 44/44 통과 |

### 오늘 만든 것 — 판정 불가 갈림길 회귀 테스트

`diagnose()` 를 통째로 돌려 **판정 / 판정 불가 두 갈래가 섞이지 않는지** 잠근다.

| 갈래 | 확인 |
|---|---|
| 판정된 응답 | 부담 구간·기여 요인·비교가 나오고 **`undecidable` 이 null** |
| 판정 불가 (양쪽 유형) | 부담 구간·기여 요인·비교·2인 보정 안내가 전부 null, 근거 목록은 여기서만 |
| 유형별 차이 | `sparse` 는 비율이 숫자, `ambiguous` 는 전부 null + 문항 수만큼 순서대로 |
| 문구 누수 | 안내문 JSON 안에 "최고부담군" 등 **구간 명칭이 한 글자도 없음** |

입력은 손으로 박지 않고 `models/parity_v1.json` 3,000건에서 갈래별 실제 응답을 골라 쓴다.
값을 고정하면 재학습 후 그 응답이 다른 갈래로 옮겨가 엉뚱한 이유로 깨진다.

**검출력을 실제로 확인했다** — 통과만으로는 울타리 역할의 증거가 되지 않아 코드를 일부러 망가뜨렸다.

| 변이 | 잡혔나 |
|---|---|
| 판정돼도 안내문을 조립하도록 변경 | ✅ `★ 근거 목록을 내지 않는다` 실패 |
| 판정 불가에도 기여 요인을 계산하도록 변경 | ✅ 양쪽 유형 `기여 요인과 비교를 내지 않는다` 실패 |

원본과 `diff` 로 차이 없음을 확인해 되돌렸다.

### 5번 결함 수정 — 판정 불가 임계값을 설정에서 읽는다 (FR-009c)

`diagnosisService` 가 임계값을 `models/uncertainty_v1.json` 에서 직접 읽고 있었다. DB의
`undecidable.thresholds` 는 기동 시 존재만 확인하고 **판정에 쓰이지 않았다.** 스펙이 요구하는
"코드 수정 없이 조정 가능"(FR-009c)이 성립하지 않는 상태였고, 09-01 에 고친 시드 값도
서비스 동작을 바꾸지 못했다.

| 바뀐 것 | 내용 |
|---|---|
| `diagnosisService.ts` | `cfg('undecidable.thresholds')` 를 읽어 판정한다 |
| `inference/undecidable.ts` | `thresholdDrift()` — 설정과 산출물이 어긋나면 문구를 낸다 |
| `server.ts` | 기동 로그에 **실제 적용 중인 임계값**을 찍고, 어긋나면 경고한다 |
| 테스트 | `undecidableThresholds.test.ts` 4건 + 어긋남 감지 5건 (87/87) |

설정이 이기게 둔 이유 — 어긋남을 기동 실패로 처리하면 설정만으로 조정할 수 없게 되어
FR-009c 와 충돌한다. 그래서 **판정은 설정을 따르고, 어긋남은 경고로 드러낸다.**

### 운영 DB 대상 검증 (읽기 전용, 09:56)

```
1) 설정 로드            OK — 필수 키 전부 존재
2) 아티팩트 정합 검증    OK — v1.0.0 (logit, 7문항), 문항집합 qs-v1.0.0, DB 문항 집합 일치
3) 적용 중인 임계값      설정 0.32391675903458894 / -2.4342723737738483
                       산출물 0.32391675903458894 / -2.4342723737738483  → 일치
4) 실제 진단 3건        판정 성립 → 중간부담군·기여요인 3·비교 4·근거목록 0
                       드문 응답 → sparse·근거목록 1·연계 있음
                       확신 부족 → ambiguous·근거목록 7·연계 있음
5) 3,000건 분포         판정 불가 60건(2.00%) = sparse 32 + ambiguous 28
                       고부담(1+2) 60.0% · 중간 27.9% · 저 9.9% · 없음 0.2%
```

**시드는 이미 재실행돼 있었다**(T029a 완료). 설정과 산출물이 일치하므로 이번 수정으로
판정 결과는 달라지지 않는다. 규정 위반만 해소되고 가시성이 생겼다.

### 원인 규명 — "어제와 다르다" 는 모델이 아니라 배포 불일치였다

모델은 바뀐 적이 없다(v1.0.0, 09-01 15:10 이후 무변경, 아카이브와 md5 동일).
갈린 지점은 **구성요소마다 서비스 중인 코드의 시점이 달랐던 것**이다.

| 구성요소 | 어제 17:30 | 오늘 10:00 | 왜 |
|---|---|---|---|
| 프론트 (:9503) | 어제 코드 | **오늘 09:13 코드** | `vite dev` 로 떠 있어 소스 수정이 즉시 반영된다 |
| 백엔드 (:9523) | 어제 코드 | **어제 17:12 코드** | `npm exec tsx src/server.ts` — **watch 모드가 아니라** 재기동 전까지 그대로 |
| DB 설정 | 어제 값 | **오늘 09:11 시드** | 문구가 두 유형 구조로 교체됨 |

**증상의 메커니즘** — 새 프론트는 응답의 `undecidable` 객체를 찾는데 옛 백엔드는 `undecidableNotice`
문자열만 보냈다. `ResultPage` 는 `v-if="r.decided"` / `v-else-if="und"` 로 갈리므로 판정 불가인
사람은 **둘 다 false 가 되어 아무 카드도 그려지지 않았다.** "판정 불가가 안 뜬다" 의 정체다.
"대부분 고부담군" 은 이와 무관한 정상값(60.0%)이고, 두 현상이 겹쳐 보였다.

**교훈** — 프론트가 빌드 산출물이 아니라 개발 서버(`vite dev`)로 운영되고 있어, 소스만 고쳐도
즉시 배포된다. 백엔드는 수동 재기동이 필요하다. **이 비대칭이 오늘 어긋남의 구조적 원인이다.**
팀장에게 알릴 것.

### 백엔드 재기동과 검증 (10:06)

```
[cb-backend] http://localhost:9523/api/v1
  모델        : v1.0.0 (logit, 문항 7개)
  문항 집합   : qs-v1.0.0
  판정 불가   : 확신도 < 0.32391675903458894 · 희소성 < -2.4342723737738483 (설정값)
                                                        ← 어긋남 경고 없음
```

운영 API 로 세 갈래 확인 (`consentTraining` 을 주지 않아 **DB 에 아무 행도 만들지 않음**, FR-031).

| 응답 | 결과 |
|---|---|
| 판정 성립 | decided=true · 중간부담군 · 기여요인 3 · 비교 4 · `undecidable` null |
| 드문 응답 | decided=false · sparse · "이번에는 상담을 권해드립니다" · 근거 1 · 연계 있음 |
| 확신 부족 | decided=false · ambiguous · 근거 7 · 연계 있음 |

구식 `undecidableNotice` 필드는 응답에서 사라졌고, 프론트가 기대하는 계약과 일치한다.

### 남은 것

1. **스펙 FR-009a 개정** — 판정 불가를 희소성 하나로만 정의하고 있어 `ambiguous`(확신 부족)가
   스펙에 없다. 구현은 두 유형인데 스펙은 한 유형이다. **팀 합의 필요**
2. ~~시드 재실행(T029a)~~ — **완료 확인.** DB 의 `undecidable.thresholds` 는 학습 산출값과 일치하고
   `undecidable.noticeText` 도 두 유형 구조로 들어가 있다 (오늘 09:11 시드 반영)
3. 프론트 `result.test.ts` 에 판정 불가 두 유형 렌더 커버리지 없음
4. 테스트 설정 문구가 `db/seeds/config.js` 값을 옮겨 적은 것이라 **시드가 바뀌어도 안 깨진다.**
   시드에서 `CONFIG` 를 export 로 분리하면 그 드리프트까지 잡히나, 팀 공용 파일 구조 변경이라 보류

---

## 되돌림 (10:19) — 이 절의 코드 작업은 현재 저장소에 없다

10:16 요청으로 **오늘 작업분을 전부 되돌렸다.** 위 기록은 무엇을 했고 왜 했는지의 이력으로 남긴다.

**되돌린 것 (오늘 09:11~09:55 변경분)**

| 종류 | 파일 |
|---|---|
| 되돌림 | `db/seeds/config.js` · `openapi.yaml` · `ResultPage.vue` · `diagnosisService.ts` · `undecidable.ts` · `server.ts` |
| 삭제 | `undecidableNotice.ts` · 테스트 4종(`undecidableNotice` · `undecidableReason` · `diagnosisBranches` · `undecidableThresholds`) |

**그대로 둔 것** — 어제 작업분(`PreSurveyPage.vue` · `App.vue` · `HomePage.vue` · `theme-claude.css` ·
`presurvey.test.ts`)과 오늘 09:06 커밋 2건(`7ac5a7d` 설정 출처 수정 · `aebcbd0` 문서). 이 둘은
**어제 작업이 오늘 아침 커밋된 것**이라 되돌리지 않았다.

**코드만 되돌리면 다시 어긋나므로 세 곳을 함께 맞췄다.**

1. 코드 → 어제 상태
2. `npm run seed:config` 재실행 → DB 문구가 옛 단일 문자열로 복원
3. 백엔드 재기동 → 되돌린 코드로 구동

검증: `판정 불가 → undecidableNotice` 문자열 정상 반환, 판정 성립 → null. 프론트(vite dev)도
되돌린 `ResultPage.vue` 를 서비스 중. 테스트 44/44 · `tsc` 통과.

**백업** — 되돌린 작업은 전부 복구 가능하다.

```
scratchpad/오늘작업-백업-1002/
  오늘-추적파일.patch          git apply 로 복원
  untracked/                   삭제한 신규 파일 5개 원본
```

되살리려면 패치를 적용하고 `untracked/` 를 제자리에 복사한 뒤, 시드 재실행과 백엔드
재기동을 다시 해야 한다(세 곳을 함께 맞춰야 한다는 점은 동일).

---

## 10:2x~ — 분석용 joblib 과 v1.0.0 전수 분석

| 시각 (KST) | 요청 | 처리 | 결과 |
|---|---|---|---|
| 10:22 | 모델 아티팩트 위치 · joblib 형태는 없는지 | `ml/train.py` 저장 방식 확인 | JSON 만 저장한다. 서버가 Node 라 joblib 을 못 읽어 **계수만 내보내고 TS 가 직접 계산**하는 구조. 대신 parity 3,000건이 두 구현 일치를 보증 |
| 10:3x | **분석용 joblib 생성** + v1.0.0 모델·데이터 분석 문서 | 재학습 없이 JSON 에서 복원 · 운영 DB 읽기 전용 분석 | `models/model_v1.joblib` (오차 3.3e-16) · `docs/HG_모델-데이터-분석-v1.0.0.md` |

### 만든 것

| 파일 | 역할 |
|---|---|
| `models/model_v1.joblib` · `models/v1.0.0/model_v1.joblib` | 분석 전용 sklearn 객체. **배포 대상 아님** |
| `ml/HG_rebuild_joblib.py` | 배포본을 덮지 않고 JSON 에서 복원. parity 3,000건으로 검증하고 불일치면 파일을 만들지 않는다 |
| `ml/HG_analyze_v1.py` | 문서의 모든 수치를 만드는 분석 스크립트 (운영 DB 읽기 전용) |
| `ml/train.py` (수정) | export 단계에서 앞으로는 학습 직후 joblib 을 함께 남긴다 |
| `docs/HG_모델-데이터-분석-v1.0.0.md` | 배포 판단용 전수 분석 |

**재학습하지 않은 이유** — `train.py --stage export` 는 `models/*.json` 을 새로 쓴다. v1.0.0 이
운영 중이라 덮을 수 없어, JSON 의 계수·범주에서 sklearn 객체를 복원하고 parity 3,000건으로
동일성을 확인했다(최대 오차 3.3e-16).

### 분석 결론 (test 602건)

```
macro F1 0.3470 · 5구간 정확도 0.4872
고부담(1~2) 재현율 0.7736 · 이진 정확도 0.6985      ← SC-005 통과
판정 불가 2.49% · 판정불가군 오분류 0.8667 > 판정군 0.5128   ← SC-016 통과
확률 보정 ECE test 0.0268                          ← 보정 불필요
```

**배포 가능**하되 발표에서는 "5구간을 맞히는 서비스"가 아니라 **"고부담을 놓치지 않는 서비스"**
로 말해야 한다. 오차가 인접 구간에 몰려 있어(실제 1번을 4·5번으로 본 것은 89건 중 1건) 안내
목적에서 위험한 방향으로 틀리지 않는다.

### 문서에 적어 둔 배포 전 위험 3가지

1. **DB `cb_model_version_v1` 의 v1.0.0 행이 14문항 평가로 오염** — 판정불가율 0.0415(실제 0.0249).
   발표에 이 표를 쓰면 틀린 숫자가 나간다
2. **프론트가 `vite dev` 로 운영** — 소스만 고쳐도 즉시 배포되는데 백엔드는 수동 재기동이라,
   오늘 오전 사고의 구조적 원인이다
3. **판정 불가 임계값을 런타임이 설정에서 읽지 않음**(FR-009c). 지금은 두 값이 같아 동작 문제는 없다

### 바로잡은 인용 하나

어제 기록의 `ECE 0.042` 는 문서에서 근거를 찾지 못해 직접 계산해 넣었다 —
**train 0.0197 / test 0.0268**(10구간 가중평균, 결정 가중치를 뺀 순수 argmax 기준).
결론(보정 불필요)은 같다.

---

## 11:0x~ — 학습·추론 라이브러리 설계와 구현 계획

| 시각 (KST) | 요청 | 처리 | 결과 |
|---|---|---|---|
| 10:50 | 학습 과정을 라이브러리로 만들 계획 | 브레인스토밍 — 경계·범용성·데이터 입력 3가지 결정 | 학습 파이프라인만 · 돌봄부담 전용 · 스냅샷 고정 |
| 11:1x | 설계 승인, md 로 저장 | `docs/HG_학습-라이브러리-설계.md` | 자체 검토에서 `train` 이 test 를 매번 소모하는 문제를 잡아 `evaluate` 를 분리 |
| 11:3x | 14문항 DB 기록 문제와 joblib 서비스 방식 질의 | 답변 후 방향 결정 | **B안으로 DB 정정 완료** · **파이썬 추론 서비스로 전환 결정** |
| 11:4x | 설계 개정 + 구현 계획 + 5회 검토 | 설계 v2 · 계획 19태스크 · 검토기록 | 아래 참조 |

### 만든 것

| 문서 | 내용 |
|---|---|
| `docs/HG_학습-라이브러리-설계.md` | v2 — 범위를 추론 서비스까지 확장 |
| `docs/HG_학습-라이브러리-구현계획.md` | Phase A 11태스크 + Phase B 8태스크, 전부 TDD |
| `docs/HG_설계-검토기록.md` | 5회 검토에서 발견·조치한 것 |

### 방향 전환 — 파이썬 추론 서비스

지금은 파이썬이 계수를 JSON 으로 뽑고 TypeScript 가 추론을 다시 구현한다. 선형 모델이라
성립하지만 **모델 계열을 바꾸면 이식 비용이 급격히 커진다.** 앞으로 모델을 바꿀 수 있다는
판단에 따라 학습한 객체(joblib)를 서비스가 그대로 쓰는 구조로 간다.

```
[Vue :9503] ── [Node :9523] ──HTTP──▶ [파이썬 추론 :9533]
```

**핵심 판단** — 판정 정책(임계값·가중치)은 Node 가 요청에 실어 보낸다. 파이썬은 상태 없는
계산기가 되고, FR-009c(설정으로 조정)가 유지되며, 설정을 바꿔도 파이썬 재기동이 필요 없다.

**장애 처리** — 파이썬이 죽으면 "지금은 진단을 드릴 수 없습니다" 로 정직하게 알리되
**기관 안내는 계속 제공**한다. 판정 불가와 같은 안전망 경로를 재사용한다.

**되돌리기** — B4 까지는 `CB_INFERENCE_URL` 로 즉시 복구할 수 있다. TS 추론 제거(B7)는
별도 승인을 받는다.

### 5회 검토에서 잡은 것

- **신설 태스크 3개** — 골든 생성(설계가 요구하는데 만드는 곳이 없었다) · 전환 전 TS↔파이썬
  3,000건 대조 · 문서와 운영 절차
- **깨질 코드 2곳** — `contributionThreshold` 출처, `modelVersion` 타입
- **새 위험 1건** — 결정 가중치를 설정으로 옮기면 산출물과 갈라질 수 있다 → `/health` 대조 경고
- **환경 문제 2건** — `pytest` 미설치, `fastapi` 가 `.pylibs` 가 아닌 사용자 계정에만 있음
- **자리표시 1건** — 프론트 테스트를 컴포넌트 단위로 구체화

### 14문항 DB 기록 정정 (11:37)

`cb_model_version_v1` 의 v1.0.0 행이 14문항 평가 결과로 덮여 있던 것을 정정했다.
값을 손으로 고치지 않고 `MODEL_VERSION=v1.0.0 python3 ml/train.py evaluate` 로 같은 코드
경로에서 다시 만들었다.

```
정정 전  F1 0.3420 · 재현율 0.7669 · 판정불가 0.0415   ← 14문항 결과
정정 후  F1 0.3416 · 재현율 0.7730 · 판정불가 0.0249   ← 7문항 v1.0.0
```

정정 전 값은 `scratchpad/cb_model_version_v1-정정전-백업.json` 에 보존했다.
14문항 평가 기록은 이 정정으로 사라졌다 — 남기려면 모델을 바꿔 끼우고 평가해야 하므로
배포 중에는 하지 않았다.

## 16:05 — 서열 모델 실험: 해봤고 기각했다

계획서 `HG_서열모델-계획.md` v2 의 11장 순서대로 실행했다.
"다섯 중 하나" 를 "예/아니오 네 번"(P(y≤1)…P(y≤4))으로 바꿔 학습하고,
같은 5-fold 에서 결정 가중치를 배포본 값으로 고정한 채 짝지어 비교했다.

```
                  지금(다항)   서열      차이     t
macro F1           0.3364    0.3337   -0.0028  -0.36
고부담 재현율        0.7844    0.7821   -0.0023  -0.74
두 칸 이상 오류       9.8%      9.5%   -0.0025  -1.81
단조성 위반            —      0.79%
```

합격 조건 넷 중 둘(재현율·F1)을 못 넘겨 **기각**했다. |t| 가 셋 다 2.776 에 한참 못 미쳐
좋아진 것도 나빠진 것도 우연과 구분되지 않는다.

계획 단계에서 "판정 불가가 2% → 11% 로 튈 수 있다" 고 걱정했는데 **반대였다** —
서열 쪽 최대확률 중앙값이 0.4856 으로 오히려 높아 문턱 아래가 1.33% 였다(지금 2.00%).
추측을 문서에 남겨 두고 실측으로 지웠다.

배포본·DB·main 브랜치 모두 손대지 않았다. 새 파일은 `ml/HG_ordinal.py` 와
`docs/HG_서열모델-결과.json` 둘뿐이고 기록은 `docs/HG_서열모델-기록.md` 에 남겼다.

정규화 튜닝 · 14문항 · 서열 모델 — **세 실험이 같은 결론으로 수렴한다.**
모델 쪽을 만져서는 이 데이터의 천장(5구간 70.6%)을 못 넘는다. 다음은 데이터 쪽이다.

## 16:18 — 라벨 품질 점검 (측정만, 판단은 보류)

서열 모델까지 기각한 뒤 "다음은 라벨 품질" 이라고 적었는데, 그게 정말 맞는 진단인지
먼저 재봤다. 스냅샷만 읽고 아무것도 쓰지 않았다 — `ml/HG_label_quality.py`.

```
라벨 ↔ 변수 상관   help_needed_hours +0.442 · understands_work_meaning -0.429
                  배포 문항 4개는 선택지 문구까지 대조 — 방향 전부 상식과 일치
7문항 중복        2명 이상 조합 326개 중 80.1% 라벨 갈림, 62.9%는 고부담 여부에서도 갈림
38변수 중복       0건 — 2,398명 전원 유일한 조합
정보량 비교        7문항 F1 0.3364 / 38변수 로지스틱 0.3141 / 38변수 RF 0.2825
```

**앞선 문서들의 근거 하나를 정정했다.** "천장 70.6%" 는 7문항 시야의 천장이지
라벨이 나쁘다는 증거가 아니다(38변수로는 중복이 0건이라 셀 수조차 없다).
그래서 "라벨이 흔들린다" 와 "문항이 부족하다" 를 가르려고 변수를 38개로 늘려 봤는데
**성능이 오르지 않았다.** 과적합 걱정이 없는 랜덤포레스트도 7문항과 같은 자리다.

결론은 한 칸만 옮긴다 — 라벨이 망가진 것도, 문항이 모자란 것도 아니고
**이 설문 도구로 설명되는 부담의 몫이 원래 작다.** 실험 셋이 전부 실패한 것과 앞뒤가 맞는다.

수렴 타당도(제외 6변수를 잣대로) · 모순 응답 · 배치 효과 · 설문 문구 검토는 **하지 않았다.**
문서 8장에 "안 함" 으로 명시해 뒀다. 특히 제외 6변수 건은 FR-004b 해석이 걸려 팀 확인이 필요하다.
재측정 신뢰도는 원자료에 재응답이 없어 영영 못 낸다.

기록 `docs/HG_라벨품질-점검.md`.

## 16:35 — 데이터 쪽 여지 두 개를 재봤다. 둘 다 아니었다

"이제 개선 여지는 데이터 쪽" 이라는 말이 세 갈래로 갈려서, 잴 수 있는 둘을 쟀다.
판정 기준은 돌리기 전에 못 박았다 — `ml/HG_data_headroom.py`.

**① 학습곡선 (더 모으면 오르나)**
```
 20%(  384건) 재현율 0.7580 · 고부담여부 정확도 66.9%
 40%(  767건)        0.7680                  68.2%
 60%(1,151건)        0.7806                  68.3%
 80%(1,535건)        0.7864                  68.8%
100%(1,918건)        0.7844                  69.4%   ← 지금
마지막 구간 +0.7%p, 폴드간 표준편차 2.9%p → 평평
```
데이터를 5배 늘리는 동안 2.5%p 올랐고 뒤로 갈수록 멈춘다. **더 모아도 소용없다.**

**③ 라벨 재정의 (5구간을 고집할 이유가 있나)**
재현율을 0.7843 으로 맞춘 뒤 정확도를 비교했다(가중치로 유리하게 못 고르게).
```
5구간(지금)  가중치1.10  재현율 0.7843  정확도 69.4%   ±0.0%p
3구간              1.60         0.7920        69.3%   -0.1%p
2분류              1.25         0.7989        70.2%   +0.8%p
```
2분류가 가장 나았지만 +0.8%p 로 기준(+2.0%p) 미달이고 폴드간 표준편차보다도 작다.
**5구간 유지.** 스펙(FR-010a·SC-004·005)을 고칠 값이 아니다.

앞선 문서의 "천장 85.0% 까지 15%p 남았다" 도 정정했다. 7문항 조합 1,156개 중
830개가 1명짜리라 천장 계산에서 자동으로 맞힌 것으로 세어진다. **느슨한 상한이지
남은 여지가 아니다.**

이로써 데이터 쪽 세 갈래 중 둘이 닫혔다. 남은 건 ② **지금 설문에 없는 변수를 새로
모으는 것** 하나뿐이고 이번 프로젝트 범위 밖이다(다음 조사 문항 설계 몫).

실험 다섯 개(정규화·14문항·서열·38변수·이번 둘)가 전부 같은 곳으로 모인다 —
**지금 배포본이 이 데이터에서 뽑을 수 있는 것에 근접해 있다.**
나쁜 소식만은 아니다. 못 맞히는 사람을 판정 불가로 빼서 상담으로 넘기는 설계가
이미 그 답이었고, 고부담 재현율 0.78 로 SC-005 하한도 넘는다.

기록 `docs/HG_데이터쪽-여지-점검.md` · 원자료 `docs/HG_데이터쪽-여지-결과.json`.

## 16:50 — 실험 여섯 개를 한 문서로 모았다

`docs/HG_실험-종합.md`. 개별 기록의 숫자를 그대로 옮기고, 14문항 실험(v1.1.0)까지
포함해 여섯 개로 정리했다.

```
1 문항 7→14개      test F1 +0.0004 · 판정불가 0.0249→0.0420(1.7배)   폐기
2 정규화 15조합     l2 C=10 +0.0200 이나 t=1.63 · 3명 계수 2배        기각
3 서열 모델        F1 -0.0028 · 재현율 -0.0023 · 전부 t<1.9          기각
4 변수 7→38개      로지스틱 0.3141 · RF 0.2825 (기준 0.3364)         기각
5 학습곡선         마지막 구간 +0.7%p < 폴드간 표준편차 2.9%p         평평
6 라벨 재정의      3구간 -0.1%p · 2분류 +0.8%p (기준 +2.0%p)          기각
```

**|t| >= 2.776 을 넘긴 실험이 하나도 없다.** 이 한 줄이 종합의 핵심이다.

6장에 실험 중 정정한 것 여섯 개를 모아 뒀다 — SEED 42→20260901, 기여 요인 클래스 지정,
L1 재현율 오독, DB v1.0.0 행 덮어쓰기, 그리고 천장 숫자에 대한 오해 둘.
7장에는 여섯 번 모두 지킨 실험 규칙 8개를 적었다. 결과보다 이게 오래 갈 것 같다.
8장은 "안 한 것" 이라 다음 사람이 했다고 오해하지 않게 했다.

결론은 **모델 성능 쪽 작업은 여기서 멈추는 것이 합리적** 이고,
유일하게 열려 있는 갈래는 지금 설문에 없는 변수를 새로 모으는 것(다음 조사 몫)이다.

## 17:45 — 판정 불가 안내 확장, 준비까지 하고 내일로

### 하려는 것
판정 불가 화면에 세 가지를 더한다. 이유(A) · 오해 방지 한 줄(B) · 다음 행동(C).

**스펙 개정이 아니라 이행이다.** FR-009a 가 이미 "판정이 어렵다는 사실과 **그 이유를**
보호자가 이해할 수 있는 말로 안내" 하라고 요구하는데 지금은 고정 문구 한 줄만 나간다.
(기억에 "FR-009a 개정 필요" 로 남아 있던 것을 정정했다.)

재료도 이미 있다 — 추론 엔진이 `sparse`(응답이 드묾) / `ambiguous`(어느 구간도 확신 못 함)
두 갈래를 내보내는데, 백엔드가 받아서 검증까지 하고 **버리고 있다**.

### 오늘 한 것 (준비만)
```
설정 백업        scratchpad/config-작업전-백업.json (20건)
되돌림 태그       HG-before-undecidable-notice
확인용 응답 확보   sparse    [4,1,5,5,5,6,2]  희소성 -2.5896 < -2.4343
                ambiguous [2,5,4,3,11,2,3] 최대확률 0.3215 < 0.3239
문구 초안        db/seeds/config.js 에 reasons·meaning·nextSteps 추가 (미커밋)
```

**DB 는 안 건드렸다. 화면은 지금 그대로다.**

### 내일 할 것
1. `diagnosisService.ts` — `undecidableReason` 을 받아 문구를 골라 응답에 싣는다(필드 3개)
2. `openapi.yaml` — 계약 반영
3. `ResultPage.vue` — 세 블록 렌더. `v-if` 라 설정에서 문구를 지우면 예전 화면으로 돌아간다
4. 테스트 — 이유별 문구 / 판정 불가에 기여 요인이 여전히 안 나오는지(FR-011c)
5. 시드 재실행 → 백엔드 재기동 → 두 화면 눈으로 확인  ← 여기서 처음 서비스가 바뀐다

### 브랜치에 대해 알아낸 것 — 앞선 판단을 정정한다

"main 이 12커밋 뒤처졌고 옮기면 presurvey 수정이 사라진다" 고 했는데 **틀렸다.**

```
git diff --name-only main experiment/HG-model | grep -E '^(backend|frontend|db)/'
  → 결과 없음.  두 브랜치의 서비스 코드 차이는 0 이다.
```

`git cherry` 로 재보니 이 브랜치의 12커밋 중 **8개는 이미 main 에 있다**(해시만 다른 같은 내용).
`docs(작업기록): 커밋 이관` 작업으로 커밋이 옮겨 심어져 **양쪽에 중복**된 상태다.
main 에 없는 건 최호준님 ML 커밋 4개뿐이고 서비스 코드는 한 줄도 없다.

그래서 **머지해도 웹서비스는 안 바뀐다.** 다만 `docs/작업기록/README.md` 한 파일에서
충돌이 나고, 머지 후 push 하면 최호준님 ML 커밋 4개가 함께 올라간다. 팀 결정 사항이라 미뤘다.

웹서비스는 브랜치가 아니라 **이 폴더의 파일**을 읽어 돌아간다는 것도 확인했다
(CI 없음, vite dev 는 즉시 반영, 백엔드는 재기동해야 반영, 설정은 기동 때 한 번).

## 18:00 — main 을 받아 왔다 (머지)

`main`(fa21f9d, 신지현 군집화 22커밋 · 이미 origin 에 푸시됨)을 우리 브랜치로 받았다.
**방향은 main → 우리 쪽**이다. 이미 푸시된 main 은 건드리지 않고 우리가 맞춰 들어갔다.

충돌은 **2건**이었다. 사전 점검에서 1건이라고 했는데 `merge-tree` 의 "changed in both" 만
봐서 **`add/add`(양쪽이 같은 이름으로 새로 만든 파일) 유형을 놓쳤다.** 다음부터 같이 본다.

| 충돌 파일 | 채택 | 이유 |
|---|---|---|
| `docs/작업기록/README.md` | 우리 판 | main 판 전체 + 최호준 줄 하나. 팀원 4명 다 남은 것 확인 |
| `docs/SJH-데이터준비와-모델선정.md` | main 판 | 신지현님이 "데이터에 구조가 없다"를 **철회·정정한 최신본**. 우리 판은 그 이전 글 |

둘 다 잃은 내용이 없고 충돌 표시도 전 파일 검색해 남은 게 없다.

중간에 `docs/JSG-프로젝트-계획서.md` 를 미추적 파일로 알고 옆으로 옮겼는데 **추적 중인
파일이었다.** 머지 후 비어 있길래 커밋본에서 복구하고 백업본과 내용이 같은 것을 확인했다.

팀원 미커밋 작업(신지현 5줄 등 21건)은 그대로 뒀다. 되돌림 지점 `HG-before-merge-main`,
백업 `scratchpad/머지전-백업/`. 푸시는 하지 않았다.

머지 후 백엔드 테스트 51건 통과, 프론트 200 · 백엔드 200 · 추론 200.

---

# 3일차 — 2026-09-03

세션 `d27a318c` · 작성자 장부 등록 완료.

## 10:26 — 모델·데이터 처리 확인과 분석 기록 신설

**요청** 진행상황 → 파이프라인 → 뷰·계열 비교 → macro F1 → 혼동행렬 → 성능 요약 →
모델의 서비스 적재 위치 → train/test 분할·결측·스케일 → **발표자용 md 정리**

**만든 것** `docs/HG-데이터분석-기록.md` (누적 기록 · 앞으로 회차를 이어 붙인다)
`ml/HG_f1_explain.py` · `ml/HG_f1_confusion.py` (읽기 전용 · train 만 사용)

**서비스·배포본·DB 를 수정하지 않았다.** test 602건도 쓰지 않았다.

### 실측으로 확인한 것

| 항목 | 결과 |
|---|---|
| train/test 분할 | 2,398 / 602 · **층화 확인**(등급 비율 train↔test 0.2%p 이내) · 폴드도 층화(등급5 = 13,12,12,12,12) |
| 결측 처리 | 대치 없음. `v_cb_tree_v1` NULL → `source_db.py` 가 `-1` → 원-핫에서 칸 하나. 38변수 중 14개에 결측, 전체 칸의 12.8% |
| 스케일 | **하지 않음.** 전 변수 범주형 → 원-핫 53열. 스케일러 사용 0건 |
| 등급별 F1 | 1:0.263 2:0.570 3:0.430 4:0.338 5:**0.091** → macro 0.3383 · weighted 0.4383 · micro 0.4629 |
| 혼동행렬 | 오류의 **90.2%가 1칸 이내**. 1등급의 87.0%가 1~2등급 안에 들어옴(화면 동작 동일) |
| 골든 테스트 | `test_engine_golden.py` **9건 통과** — joblib ↔ JSON 3,000건 오차 1e-9 미만 |

### 앞선 판단을 정정한 것 세 가지

| 정정 전 | 실제 |
|---|---|
| "트리 뷰·선형 뷰를 계열별로 나눠 썼다" | **`v_cb_tree_v1` 하나만 쓴다.** `v_cb_linear_v1` 은 DB에 3,000행으로 존재하나 **읽는 코드가 0건**이다. 결측 `-1` 치환은 `source_db.py:57` 이 하고, 로지스틱은 원-핫이 `*_isna` 와 같은 효과를 낸다. `tasks.md` T025 가 이와 어긋나 있다(팀 판단으로 미수정) |
| "`CB_INFERENCE=ts` 로 판정 경로를 되돌릴 수 있다" | **그 스위치는 `9250d0c` 에서 제거됐다.** 경로는 파이썬 하나뿐이고 자동 폴백도 없다. CLAUDE.md 75·89행과 `npm run test:parity` 가 코드와 어긋난 상태다 |
| "배포본 결정 가중치 `[1.2, ...]`" | **`[1.1, 1.1, 1, 1, 1]`** 이다. 1.2는 `baseline` 단계(38변수)의 값이었다 |

### 모델 적재 경로 — 위치까지 확인

```
① 파이썬 추론 서비스   engine.py:60  joblib.load(model_v1.joblib)   ← 계산하는 유일한 곳
                      engine.py:77  encoder.transform → predict_proba
② 백엔드              modelLoader.ts:28-32  JSON 5개만 읽는다 (joblib 안 읽음)
                      기동 시 4단계 대조 후 불일치면 기동 실패
```

**두 프로세스 모두 최상위 `models/`** 를 본다(`v1.0.0/` 이 아니다). 두 폴더의 파일 7개는
sha256 전부 동일해 현재 문제는 없다. 다만 `models/current.json` 이 없고 런타임이 그
파일을 읽지도 않으므로 — **`promote` 로 새 릴리스를 만들어도 서비스는 바뀌지 않는다.**
`v1.1.0`·`v1.2.0` 에는 `model_v1.joblib` 이 없어 `--release` 로 가리키면 기동에 실패한다.

**JSON 이 원본이고 joblib 이 복원본이다** — `HG_rebuild_joblib.py` 가 09-02 10:46 에
배포 JSON 에서 되살린 것이고, 계수 최대 차이 0.0 을 직접 확인했다.

### 되돌리기(TS 추론 복원)를 검토했으나 하지 않기로 함

**사용자 판단으로 현 구조 유지.** 검토 결과만 남긴다.

- 역적용 충돌 없음(`git apply --check -R`), `7c2f9d4` 가 복구 지점, 이후 해당 파일 변경 0건
- `modelLoader.ts`·`types.ts` 는 **변경 0** — 모델을 읽는 형식은 그대로다
- 실제 범위는 8개 파일(`server.ts`·`package.json`·parity 테스트 포함)
- **위험** — `.env` 에 `CB_INFERENCE` 가 없어 기본값 `ts` 로 떨어진다. 코드만 되돌리고
  재기동하면 판정 주체가 조용히 바뀌고, `server.ts` 의 문항집합 대조 가드가
  `if (mode === 'http')` 안으로 들어가 **꺼진다.** 되돌린다면 `.env` 한 줄이 세트다
- 백엔드는 `tsx watch` 가 아니라 `tsx` 로 떠 있어 **파일 저장만으로는 재기동되지 않는다**

### 남긴 메모

`run/backend.pid` 의 값(84586)이 실제 프로세스(83410)와 다르다. 재기동 때 그 파일을
보고 죽이면 엉뚱한 프로세스를 건드린다.

## 10:36 — 분석을 4단계(기술→관계→인과→예측)로 재조직

**요청** "1 기술적 요약 2 관계 기술 3 인과 설명 4 예측 순서로 진행되는 것이니
이 토대로 작성해 이어서 기록해 달라"

1회차에서 잰 것은 **①기술·④예측뿐**이었다. **②관계와 ③인과 한계를 처음 측정**하고
`docs/HG-데이터분석-기록.md` 에 2회차로 이어 붙였다. `ml/HG_relations.py` 신설
(읽기 전용 · train 2,398건만 · test 미사용).

### 새로 잰 것

| 측정 | 결과 |
|---|---|
| 순위상관(스피어만) | `help_needed_hours` **+0.442** · `understands_work_meaning` **−0.429** · `can_work_standard_job` −0.396 · `family_support_for_employment` +0.247 · `daily_routine_satisfaction` +0.241 |
| 방향 검증 | 선택지 문구까지 대조해 **네 문항 모두 상식 방향과 일치**. 뒤집힌 것 없음 |
| 조합 구조 | 서로 다른 조합 1,156개 중 **830개(71.8%)가 1명짜리** |
| 라벨 일관성 | 2명 이상 조합 326개 중 **라벨 갈림 80.1% · 고부담 여부 갈림 62.9%** (기존 문서 값 재현) |
| **상한(신규)** | 조합별 최빈 라벨 기준 — 전체 72.3%(부풀림) / **2명 이상 조합만 57.7%(정직한 상한)** / 고부담 2분류 77.1% |
| **상한 대비 위치(신규)** | 같은 집단에서 모델 5등급 **47.3%** vs 상한 57.7% (10.4%p) · 고부담 **71.6%** vs 77.1% (5.5%p) |

### 해석에서 조심한 것

- **10.4%p 를 "남은 개선 여지"로 쓰지 않았다.** 상한은 학습 답을 통째로 외웠을 때의
  값이고, train 에서도 조합의 71.8%가 1명짜리라 외운 모델은 실이용자에게 무력하다.
  **"암기로 닿는 거리"이지 일반화로 닿는 거리가 아니다**라고 명시했다.
- **인과 주장의 경계를 먼저 그었다.** 관찰 설문이라 "이 요인이 부담의 원인"이라고 말할
  수 없다. 다만 **개입 실험 6건**(문항 수·변수 수·데이터량·모델 계열 등을 우리가 직접
  바꾼 것)은 인과 주장이 가능하므로, "그것들이 병목이 아니다"까지는 정당하다고 정리했다.
- 배포 7문항 중 `relation_to_person`(+0.051)·`household_head_type`(+0.032)은 **단독
  상관이 거의 0인데도 선별에 살아남았다.** 조합에서 값을 낸다는 뜻이라, 상관만 보고
  문항을 빼면 안 된다는 점을 기록했다.

발표 질문 3개를 추가했다 — "원인인가요" · "성능을 더 올릴 수 있나요" · "그럼 무엇을".

**서비스·배포본·DB 미수정. test 602건 미사용.**

## 11:22 — 스냅샷 설명과 시스템 전체 구조 시각화

**요청** "스냅샷에 대해 알려주고 전체적인 구조를 시각화해서 `HG-데이터분석-기록.md` 에
이어서 작성해 달라"

3회차로 추가했다(문서 594 → 879줄). 다이어그램 5장 — 전체 지도 · 학습 파이프라인과
관문 · 요청 한 건의 경로 · 데이터가 사는 곳 · 어긋남을 막는 장치.

### 스냅샷 — 코드를 읽고 실물로 대조한 것

| 확인 | 결과 |
|---|---|
| 파일 | `data/snapshots/cb_dataset_2026-09-01.csv.gz` 50KB · **3,001줄**(헤더 1 + 3,000행) + `.meta.json` 323B |
| 해시 대조 | 실제 파일 sha256 = meta 기록값 **일치** (`2f88db8e…1451ba`) |
| 열 구성 | `split · cv_fold · care_burden` + 38변수 = 41열 |
| 결측 | **스냅샷 시점에 이미 `-1` 로 치환**되어 파일에 그대로 보인다. 학습 단계가 아니다 |
| 덮어쓰기 | `snapshot.py:40` 에서 금지 — 덮으면 과거 `run.json` 의 해시와 갈라진다 |
| 무결성 | train 2398 · test 602 · 변수 38 · fold [1~5] · **배제 6변수 누수** 5검사. `assert` 가 아니라 예외(`python -O` 에서도 살아 있어야 함) |
| 비밀번호 | meta 에 `host·port·database` 만 담는다(원칙 III) |

### 저장소 실측 행 수 (오늘 기준)

```
cb_dataset_v1 3,000 · v_cb_tree_v1 3,000 · v_cb_linear_v1 3,000(읽는 코드 0건)
cb_facility_v1 875 · cb_facility_service_v1 1,277 · cb_region_v1 229(기관 있는 곳 221)
cb_event_log_v1 1,864(세션 212) · cb_training_response_v1 11
```

### 새로 기록한 유의사항 2건

- **⑤ 기관 수를 1,283으로 말하면 부풀려진다.** 원본 1,283건은 "기관 × 서비스 종류"
  단위이고 합치면 **875개 기관**(1종 473 · 2종 402). 전화 결측도 58건이 아니라 **40건**.
  **발표자료에서 이 숫자를 쓰고 있다면 고쳐야 한다.**
- ⑥ 스냅샷의 `cv_fold` 는 test 를 `0` 으로 적는다(DB 는 NULL). `source_db.py` 의
  `int(r[1] or 0)` 때문이며 폴드 반복은 train 만 도므로 계산 영향은 없다.

발표 질문 4개 추가 — 재현성 · 데이터 변경 시 · 누수 방어 · 기관 수.

**읽기 전용. 서비스·배포본·DB 미수정, test 602건 미사용.**

## 12:48 — 결정 가중치 1.1 의 근거와 민감도 (4회차)

**요청** "지금 배포중인 버전에서 가중치를 1.0으로 바꾸면 어떤 영향이 있을까" → 4회차 기록

`ml/HG_weight_sensitivity.py` 신설(읽기 전용 · train OOF · test 미사용).
확률은 가중치와 무관하므로 **같은 확률에 가중치만 바꿔** 비교했다.

### 1.1 → 1.0 의 영향

| | 1.1 | 1.0 | 차이 |
|---|---|---|---|
| macro F1 | 0.3383 | 0.3377 | −0.0006 |
| **고부담 재현율** | 0.7843 | 0.7490 | **−0.0353** |
| 고부담 판정 수 | 1,474 | 1,389 | −85 |
| 고부담 정밀도 | 0.6934 | 0.7027 | +0.0093 |

등급이 바뀌는 사람 **85명(3.54%)** 이고 전부 한 방향이다(2→3 83명 · 2→4 1명 · 1→3 1명 ·
반대 방향 0명). 그 85명 중 **실제 고부담이 46명**, 실제 비고부담이 39명이다.

**결론 — 바꾸지 않는다.** 얻는 것이 사실상 0인데 실제 고부담 46명이 안내를 못 받는다.

### 확인한 것

- **판정 불가 비율은 가중치와 무관하다.** `engine.decide` 가 `maxp` 를 **가중치 적용 전
  확률**로 잡는다(`engine.py:90-92`). 2.49% 는 그대로다
- 판정에 실제로 쓰이는 값은 아티팩트가 아니라 **`cb_config_v1.model.decisionWeights`** 다.
  현재 설정 `[1.1,1.1,1,1,1]` 로 아티팩트와 일치. 바꾸려면 DB 한 줄 + 백엔드 재기동이고
  재학습은 필요 없다

### 1.00~1.60 훑어본 결과 — 새로 나온 것

재현율은 가중치를 올릴수록 단조 증가하고, macro F1 은 **1.10 에서 꼭대기**를 찍고 내려온다.
`tune_decision_weights()` 가 고른 값이 실제 최댓값임을 재확인했다.

**다만 1.15 가 눈에 띈다** — `F1 0.3382(−0.0001) · 재현율 0.7951(+0.0108)`.
**F1 을 거의 잃지 않고 재현율을 1.1%p 더 얻는다.** 고부담을 놓치지 않는 것이 목적이라면
더 나은 값일 수 있다.

**그러나 지금 근거로는 바꾸지 않는다** — train OOF 한 번 잰 값이고 폴드별 분산을
보고하지 않았다(1회차 유의사항 ③과 같은 문제). 0.0001 은 잡음 안이다. **그리고 test
602건은 이미 소진했다.** 바꾸려면 폴드별 표준편차부터 재야 한다.

발표 질문 3개 추가 — "왜 1.1인가" · "가중치를 주면 편향 아닌가" · "바꾸면 재학습하나".

**읽기 전용. 서비스·배포본·DB 미수정, test 미사용.**

## 13:5x — 제출용 산출물 6종 중 5종 생성 (옵션 C)

**요청** 필수 산출물 6종 점검 → 옵션 C(Pipeline 은 확률까지, 나머지는 predict.py)로 진행.
**2번(최종 노트북)은 나중에 하기로 하고 제외.**

### 만든 것

```
deliverables/model.joblib   4,281 B   Pipeline(OneHotEncoder → LogisticRegression)
deliverables/policy.json    3,791 B   가중치 · 판정불가 임계값 · 빈도표 · 기여도 기준
deliverables/schema.json    6,210 B   문항 · 타입 · 허용범위 · 결측 규칙
deliverables/predict.py     8,323 B   predict(payload) -> dict
deliverables/model_card.md 11,332 B   용도 · 성능 · 사용 금지 8항
README.md                    220줄   문제정의 · 구조 · 실행 · 데이터 · 제출물
ml/HG_build_deliverables.py           생성 (재학습 없음)
ml/HG_verify_deliverables.py          3,000건 대조
```

**요구 6종에 없는 `policy.json` 이 하나 늘었다.** Pipeline 이 임계값·빈도표·가중치를 담지
못하는데, `predict.py` 에 숫자를 박으면 모델을 바꿀 때 조용히 갈리기 때문이다.

### 재학습하지 않았다

`train --stage export` 를 돌리면 운영 중인 `models/*.json` 이 덮여 쓰인다. 그래서
**기존 `model_v1.joblib` 에서 `encoder`·`model` 객체를 그대로 꺼내 Pipeline 에 끼웠다**
(`HG_rebuild_joblib.py` 와 같은 이유). 생성 시 두 가지를 검증하고, 어긋나면 파일을 만들지 않는다.

```
검증① Pipeline 확률 vs parity 3,000건   최대 오차 3.331e-16
검증② 계수·절편 차이 0.0 · 범주·spans 일치
```

### 두 벌이 갈리지 않았는지 3,000건 대조

`engine.py` 와 `predict.py` 가 같은 계산의 두 벌이 됐다(09-02 에 TS 이식본을 지운
그 문제와 같은 구조). `HG_verify_deliverables.py` 가 `parity_v1.json` 으로 대조한다.

```
대조 3,000건 (판정 성립 2,940 · 판정 불가 60)
   확률 3.331e-16 · 희소성 0.0 · 기여도 0.0 · 기준값 0.0
   판정 불가 불일치 0건 · 등급 불일치 0건        → 통과
```

### 작업 중 드러난 것 — 1회차 유의사항 ①이 실제로 걸렸다

`parity` 대조에서 **"문항 2는 '해당 없음'을 받지 않습니다"** 로 막혔다. 학습 데이터에는
1·2번 문항에 결측(-1)이 3건씩 있는데 **서비스 화면에는 그 선택지가 없기 때문**이다.
스키마가 모델의 실제 정의역보다 좁았던 것이라 두 개념을 나눴다.

```
nullable        서비스 화면이 '해당 없음' 선택지를 주는가        (5번만 true)
acceptsSentinel 모델이 결측(-1)을 학습한 범주로 갖는가          (1·2·5번 true)
```

`null` 은 nullable 문항에서만, `-1` 직접 입력은 acceptsSentinel 문항에서만 받는다.
후자는 **학습 데이터 재현용**이며 실서비스에서는 나오지 않는 값임을 스키마와 모델카드에 적었다.

### 검증 결과

```
ml/tests            112 passed          (기존 테스트 무손상)
models/             git status 비어 있음 (배포본 무변경)
프론트 9503 200 · 백엔드 200 · 추론 200  (서비스 무영향, 재기동 없음)
```

### 남은 것

- **2번 최종 노트북** — 접속정보 제거(SJH 7건 등) · DB 의존을 스냅샷 읽기로 전환 ·
  클린 런타임 무오류 검증. `tasks.md` T098 과 겹친다
- `requirements.txt` 사본을 `deliverables/` 에 둘지 미결정. 지금은 경로만 안내

## 14:08 — 제출 패키지 README 추가와 독립 실행 검증

**요청** "policy.json 은 뭐하는 파일이냐 · README 도 그 폴더에 넣어 달라"

`deliverables/README.md` 신설(250줄). 루트 README 를 그대로 복사하지 않고 **패키지만
떼어 받은 사람 기준으로 다시 썼다** — 상대 링크가 깨지고, 저장소에만 있는 경로를
안내하게 되기 때문이다.

| 바꾼 것 | 이유 |
|---|---|
| 의존 버전을 **인라인으로 명시** | `ml/requirements.txt` 가 패키지에 없다. `pip3 install scikit-learn==1.6.1 …` 을 직접 적었다 |
| 실행 절이 서비스 기동 → **`predict()` 사용법**으로 | 받는 사람이 할 일은 서비스 기동이 아니다 |
| 입력 규칙 표 추가 | `value` / `null` / `-1` 세 표기와 허용 문항을 명시 |
| 척도 방향 표를 앞으로 | 1이 최고부담인 역방향이라 가장 실수하기 쉽다 |
| 파일이 나뉜 이유 절 추가 | Pipeline 이 ②③④를 담지 못한다는 설명 |

**루트 README 와 내용이 겹친다.** 한쪽만 고치면 갈리므로, 모델 수치가 바뀌면 두 파일을
같이 봐야 한다(작업기록에 남긴다).

### 독립 실행 검증 — 저장소 밖으로 떼어내 확인

패키지를 저장소와 무관한 폴더로 복사해 실행했다. **README 의 "이 폴더만으로 재현할 수
있다"가 실제로 성립한다.**

```
판정 성립 사례   등급 1 · 기여요인 3개 정상
판정 불가 사례   sparse · rarity −2.5896 < −2.4343
                등급·기여요인 모두 None (구간도 근거도 내지 않음)
```

### policy.json 이 담는 것 (질문 답변)

Pipeline 이 담지 못하는 값들이다. `predict.py` 에 숫자를 박으면 모델 교체 시 조용히
갈리므로 파일로 뺐다.

```
decision_weights [1.1,1.1,1,1,1]        가중치
undecidable      tau_conf 0.32392 · tau_dens −2.43427 · eps 1e-4
                 freq_table 7변수 53범주      ← joblib 에 없다. 희소성 계산에 필수
contribution     min_threshold 0.132502 · top_k 3
                 expected_contrib 7×5         ← 기여도 분해의 기준값
메타             model_version · question_set_version · sklearn_version · classes · features
```

**계수·범주·spans 는 넣지 않았다.** Pipeline 을 단일 출처로 삼아 `predict.py` 가
`enc.categories_`·`clf.coef_` 에서 유도한다. 중복 저장하면 갈릴 수 있기 때문이다.
생성 시 검증②가 이 유도값이 `model_v1.json` 과 같은지 대조한다(차이 0.0).

## 15:1x — 최종 노트북 신설 (학습 실행 포함) · 세 경로 무오류 검증

**요청** "(데이터연결 → 데이터 준비 → 모델학습 → 서비스) 전체 과정을 클린 런타임에서
무오류로 도는 노트북을 새로 만들어 달라. 학습 실행 포함."

```
deliverables/최종-전체과정.ipynb   셀 23개 (코드 13 · 설명 10)
ml/build_final_notebook.py         노트북 생성기 (손으로 고치지 않고 여기서 만든다)
ml/verify_final_notebook.sh        세 경로 헤드리스 실행 검증
```

> **15:1x 이후 `notebooks/` → `deliverables/` 로 옮겼다**(사용자 지시). 6종을 한 묶음으로
> 낸다. 옮긴 위치에서 세 경로를 다시 돌려 무오류를 재확인했다.

### 기존 노트북 5개를 고치지 않고 새로 썼다

`모델-학습-과정.ipynb`·`HG-모델-성능-리포트.ipynb` 가 **구 `ml/train.py` 를 임포트**하는데,
그 모듈은 `models/` 최상위에 직접 쓴다(`train.py:360·401`). **검증한다고 돌렸다가 운영
중인 v1.0.0 을 덮어쓸 위험**이 있어 새 노트북은 `cb_burden` 만 쓴다. 5개 전부 DB 직결이고
4개에 평문 접속정보가 남아 있는 것도 이유다(T098과 겹침 · 기존 파일은 손대지 않았다).

### 노트북이 지키는 규칙

| 규칙 | 구현 |
|---|---|
| 배포본 무변경 | 0절에서 `models/*.json` 6개 sha256 을 찍고 6절에서 재대조. 다르면 예외 |
| test 미사용 | `evaluate` 를 부르지 않는다. 교차검증만 보고 최종 성능은 기록값을 인용 |
| 학습 격리 | `cb_burden train` → `models/runs/<run_id>/` 에만 |
| DB 없어도 완주 | `.env` 있으면 뷰 확인만, 학습 입력은 고정 스냅샷 |
| 접속정보 미기재 | `.env` 에서만 읽는다 |
| 의존 최소화 | **pandas·matplotlib 안 씀** — 클린 환경 폰트·버전 사고를 없앰 |

강제 스위치 둘을 넣었다 — `CB_NO_DB=1`(오프라인), `CB_NO_SERVICE=1`(서버 없음).
채점 환경을 여기서 재현해 볼 수 있다.

### 검증 — 실제로 헤드리스 실행했다

`jupyter`·`nbconvert`·`ipykernel` 이 저장소에 없어 **별도 venv(파이썬 3.9.6)** 를 만들어 돌렸다.

```
① 채점자 환경  DB 없음 · 서버 없음   → 오류 0  (predict.py 폴백으로 판정)
② 오프라인    DB 없음 · 소켓 있음   → 오류 0
③ 평소 환경    DB 있음 · 소켓 있음   → 오류 0  (v_cb_tree_v1 3,000행 확인)
```

**재현성이 확인됐다** — 노트북이 학습한 모델과 배포본의 차이가 `계수 0.000e+00 ·
절편 0.000e+00`, 임계값도 `0.323917 / −2.434272` 로 동일하다. 학습 20.4초.

테스트로 생긴 `models/runs/2026-09-03T*` 3건은 지웠다. 배포본·서비스 무영향 확인
(models 무변경 · 프론트 200 · 백엔드 200 · 추론 200).

### 확인 중 발견 — `ml/requirements.txt` 가 설치 불가능하다

```
pymysql==1.4.6      PyPI 최신은 1.2.0. 이 버전은 없다 (pip 26.0.1 로 재확인)
나머지 6개 핀        전부 실재 확인 (fastapi·uvicorn·pydantic·pytest·httpx·joblib·scipy)
```

`pip install -r ml/requirements.txt` 가 **그 줄에서 실패한다.** 설치된 pymysql 은
`.pylibs` 가 아니라 사용자 site-packages 에 있고, `pymysql.__version__` 은 패키지
버전이 아니다(1.2.0 을 깔아도 2.2.8 로 찍힌다). **루트 README 가 이 명령을 안내하고
있어 고쳐야 한다.** 공유 파일이라 손대지 않고 보고만 한다.

## 15:2x — 최종 노트북을 deliverables/ 로 이동

**요청** "deliverables로 옮겨줘"

```
notebooks/최종-전체과정.ipynb  →  deliverables/최종-전체과정.ipynb
ml/build_final_notebook.py       출력 경로 수정
ml/verify_final_notebook.sh      대상 경로 수정
```

옮긴 위치에서 **세 경로(채점자·오프라인·평소)를 다시 돌려 무오류 재확인**했다.
테스트 실행 폴더도 정리했다.

### README 의 주장이 깨져서 같이 고쳤다

`deliverables/README.md` 가 **"이 폴더만으로 예측을 재현할 수 있다"** 고 적고 있었는데,
노트북은 `ml/cb_burden`·`models/`·`data/snapshots/` 를 읽으므로 **저장소 전체가 필요하다.**
그대로 두면 문서가 거짓이 된다.

| 고친 곳 | 내용 |
|---|---|
| 머리말 | "예측은 이 폴더만으로" + "노트북만 저장소 필요" 로 분리 |
| §2 파일 목록 | 노트북 추가 · "앞의 여섯은 폴더만으로 돌아간다. 노트북 하나만 예외다" |
| **§7 신설** | 노트북 6절 구성 · 필요한 것 · 안전 규칙 4가지 · 검증 결과표 · `CB_NO_DB`/`CB_NO_SERVICE` 사용법 |

제출물이 7개 파일이 됐다(요구 6종 + `policy.json`).
