# 학습 / 테스트 데이터 뷰(View) 및 분할 상태 명세서

> **이 문서는 [SJH-데이터준비와-모델선정.md](./SJH-데이터준비와-모델선정.md) 로 통합되었다.** (2026-09-02, 신지현)
>
> 내용이 그쪽 **§1~§3** 에 그대로 들어갔고, 그 과정에서 DB 실측과 대조해
> 아래 두 군데를 바로잡았다.
>
> | 이 문서에 있던 서술 | 실측 |
> |---|---|
> | 설명변수 **37개** | **38개** (`ml/db.py` 의 `assert len(features)==38`) |
> | `v_cb_linear_v1` 컬럼 **43개**, 결측 지시자 **1개** | **44개**, 지시자 **2개** (`past_job_count_isna`, `age_disability_suspected_isna`) |
>
> 원본 전문은 `docs/_backup/2. 학습테스트 데이터 뷰(view)와 분할 상태_설명.20260902.bak.md` 에 있다.
> 컬럼 하나하나의 정의는 [학습데이터셋-컬럼정의.md](./학습데이터셋-컬럼정의.md) 를 계속 본다.
