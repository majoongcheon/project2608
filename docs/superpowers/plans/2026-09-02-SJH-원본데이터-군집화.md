# 원본 507컬럼 군집화 — 구현 계획 (A안)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 원본 조사 507컬럼에서 분기 결측을 게이트 변수로 승격시킨 뒤 Gower 거리로 군집화하고, **널 베이스라인과 비교해** 이 데이터에 군집 구조가 있는지 없는지를 확정한다.

**Architecture:** 순수 함수 모듈 7개(`load`·`clean`·`gates`·`scales`·`gower`·`cluster`·`score`)를 얇은 실행 스크립트가 엮는다. 각 모듈은 numpy 배열과 파이썬 자료구조만 주고받고 파일·DB 를 직접 만지지 않는다(`load` 만 예외). 그래서 단위 테스트가 DB 없이 돈다.

**Tech Stack:** Python 3.11 · numpy · scipy(계층군집) · scikit-learn(실루엣·ARI·NMI) · pymysql(조인용 1회 조회) · pytest. **새로 설치하는 패키지 없음** — Gower 와 PAM 은 직접 구현한다.

**이 계획의 범위:** 스펙 §10 의 **1일차 = A안** 전체다. B안(하위 문항까지 Gower)·C안(2층 군집화)은 A 결과를 보고 별도 계획으로 세운다.

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `ml/sjh_cluster/__init__.py` | 빈 파일 (패키지 선언) |
| `ml/sjh_cluster/load.py` | 원본 CSV 읽기, DB 에서 `split`·`cv_fold` 가져와 값 튜플로 조인 |
| `ml/sjh_cluster/clean.py` | 버릴 컬럼 판정 (전부결측·상수·식별자·주관식·소수 블록) |
| `ml/sjh_cluster/gates.py` | 결측 패턴이 같은 컬럼을 묶어 게이트 이진 변수 생성 |
| `ml/sjh_cluster/scales.py` | 컬럼별 척도 판정 (binary/nominal/ordinal/continuous) |
| `ml/sjh_cluster/gower.py` | Gower 거리 행렬. pairwise 결측 제외 + 블록 가중치 |
| `ml/sjh_cluster/cluster.py` | PAM(k-medoids) 직접 구현 + 계층군집(scipy) |
| `ml/sjh_cluster/score.py` | 실루엣·ARI·NMI·교차표·고부담 리프트·게이트 오염도 |
| `ml/SJH-cluster-raw.py` | 실행 엔트리. 위 모듈을 순서대로 호출하고 JSON 을 쓴다 |
| `ml/tests/test_sjh_cluster.py` | 위 모듈들의 단위 테스트 |

**왜 스펙(§8)의 단일 스크립트에서 바꿨나** — Gower 와 PAM 을 직접 구현하므로 **그 정확성을 테스트로 못 박아야 한다.** 한 파일에 다 넣으면 테스트가 DB 와 3,000행 CSV 에 묶여 돌릴 수 없다. 산출물 이름(`ml/SJH-cluster-raw.py`)은 스펙대로 유지한다.

---

## 사전 준비

워크트리 `~/project2608-sjh` 에는 **`.gitignore` 로 제외된 것과 미추적 파일이 없다.** 원본 CSV 와 파이썬 라이브러리를 가져와야 한다.

### Task 0: 워크트리 준비

**Files:**
- Create: `ml/.pylibs` (심볼릭 링크), `ml/.pylibs-sjh` (심볼릭 링크), `data/snapshots/2024.csv` (복사)

- [ ] **Step 1: 라이브러리를 심볼릭 링크로 연결**

358MB 를 복사하지 않는다. `.gitignore` 의 `ml/.pylibs*/` 가 둘 다 막으므로 커밋될 위험이 없다.

```bash
cd ~/project2608-sjh
ln -s ~/project2608/ml/.pylibs     ml/.pylibs
ln -s ~/project2608/ml/.pylibs-sjh ml/.pylibs-sjh
```

- [ ] **Step 2: 원본 CSV 복사**

```bash
cp ~/project2608/data/snapshots/2024.csv data/snapshots/2024.csv
```

- [ ] **Step 3: `.env` 를 링크 (DB 조회용)**

```bash
ln -s ~/project2608/.env .env
```

- [ ] **Step 4: 동작 확인**

Run:
```bash
cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs python3 -c "
import numpy, scipy, sklearn, pymysql, csv
rows = sum(1 for _ in open('data/snapshots/2024.csv', encoding='utf-8-sig'))
print('numpy', numpy.__version__, '· rows', rows)"
```
Expected: `numpy <버전> · rows 3001`

- [ ] **Step 5: 미추적 파일이 커밋 대상이 아닌지 확인**

Run: `git status --porcelain`
Expected: `?? data/snapshots/2024.csv` 만 보이고 `.pylibs` 링크는 안 보인다.

- [ ] **Step 6: `2024.csv` 를 이 브랜치에서 무시하도록 등록**

원본 조사 파일 3.2MB 는 커밋하지 않는다. 공유 폴더에서도 미추적 상태다.

```bash
printf '\n# 원본 조사 파일 — 커밋하지 않는다 (공유 폴더에서 복사해 쓴다)\ndata/snapshots/2024.csv\n' >> .gitignore
git add .gitignore
git -c user.name="신지현" commit -m "chore(gitignore): 원본 조사 CSV 는 커밋하지 않는다

data/snapshots/2024.csv 는 3.2MB 원본 조사 파일이고 공유 폴더에서도
미추적 상태다. 실험 워크트리에서는 복사해 쓰고 저장소에는 넣지 않는다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 1: 원본 로드와 조인 (S1)

원본 CSV 3,000행에 `split`·`cv_fold` 를 붙인다. `row_key` 가 행 내용 MD5 라 원본 `ID` 와 대응하지 않으므로 **38변수 + target 값 튜플**로 조인한다.

**Files:**
- Create: `ml/sjh_cluster/__init__.py`
- Create: `ml/sjh_cluster/load.py`
- Test: `ml/tests/test_sjh_cluster.py`

- [ ] **Step 1: 패키지 선언 파일 생성**

```bash
mkdir -p ml/sjh_cluster
printf '"""원본 507컬럼 군집화 실험 (신지현, 2026-09-02)."""\n' > ml/sjh_cluster/__init__.py
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`ml/tests/test_sjh_cluster.py` 를 만들고 아래를 넣는다.

```python
# -*- coding: utf-8 -*-
"""원본 507컬럼 군집화 실험 단위 테스트.

DB 와 3,000행 CSV 없이 돌아야 한다. 모든 입력은 이 파일 안에서 만든다.
"""
import numpy as np
import pytest

from sjh_cluster import load


def test_join_by_value_tuple_붙인다():
    """값 튜플이 유일하면 split 이 정확히 붙는다."""
    raw = [
        {'ID': '1', 'A1': '1', 'G6': '3', 'I13': '2'},
        {'ID': '2', 'A1': '2', 'G6': '4', 'I13': '5'},
    ]
    db = [
        {'relation_to_person': 2, 'help_needed_hours': 4, 'care_burden': 5,
         'split': 'test', 'cv_fold': None},
        {'relation_to_person': 1, 'help_needed_hours': 3, 'care_burden': 2,
         'split': 'train', 'cv_fold': 3},
    ]
    mapping = {'relation_to_person': 'A1', 'help_needed_hours': 'G6'}

    out = load.join_split(raw, db, mapping)

    # raw[0] 은 (A1=1, G6=3, I13=2) → db[1] 과 일치
    assert out[0]['__split'] == 'train'
    assert out[0]['__cv_fold'] == 3
    assert out[1]['__split'] == 'test'


def test_join_중복_튜플이면_예외():
    """같은 값 조합이 둘 이상이면 조인이 모호하므로 멈춘다."""
    raw = [{'ID': '1', 'A1': '1', 'I13': '2'}, {'ID': '2', 'A1': '1', 'I13': '2'}]
    db = [{'relation_to_person': 1, 'care_burden': 2, 'split': 'train', 'cv_fold': 1},
          {'relation_to_person': 1, 'care_burden': 2, 'split': 'test', 'cv_fold': None}]

    with pytest.raises(load.JoinError, match='중복'):
        load.join_split(raw, db, {'relation_to_person': 'A1'})


def test_join_매칭_실패하면_예외():
    raw = [{'ID': '1', 'A1': '9', 'I13': '2'}]
    db = [{'relation_to_person': 1, 'care_burden': 2, 'split': 'train', 'cv_fold': 1}]

    with pytest.raises(load.JoinError, match='매칭'):
        load.join_split(raw, db, {'relation_to_person': 'A1'})


def test_join_DB에_없는_컬럼이_매핑에_있으면_예외():
    """컬럼정의서와 DB 스키마가 어긋나면 조용히 넘기지 않는다."""
    raw = [{'ID': '1', 'A1': '1', 'I13': '2'}]
    db = [{'relation_to_person': 1, 'care_burden': 2, 'split': 'train', 'cv_fold': 1}]

    with pytest.raises(load.JoinError, match='DB 에 없는 컬럼'):
        load.join_split(raw, db, {'없는컬럼': 'A1'})
```

- [ ] **Step 3: 테스트가 실패하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'sjh_cluster.load'`

> **실행 중 발견 (2026-09-02)** — 조인에는 **결측 정규화가 필요하다.** 원본의 `999`(나이 무응답)를
> DB 는 `NULL` 로 바꿔 저장했으므로 문자열 그대로 비교하면 매칭이 실패한다. `load.SENTINELS` 로
> `docs/학습데이터셋-컬럼정의.md` §② 의 규칙을 적용한다. 코드값만 보고 일괄 처리하면 안 된다 —
> `secondary_caregiver_type` 의 `9` 는 "9. 없음"이라는 유효한 보기이고 518건이다.
> 테스트 2건(`무응답코드는_NULL_로_통일된다`, `유효한_9는_NULL_이_아니다`)을 더했다.

- [ ] **Step 4: `load.py` 구현**

```python
# -*- coding: utf-8 -*-
"""원본 조사 CSV 를 읽고 기존 train/test 분할을 붙인다.

`cb_dataset_v1.row_key` 는 행 내용 MD5 라 원본 CSV 의 `ID` 와 대응하지 않는다.
그래서 38변수 + target 의 **값 튜플**로 조인한다. 값이 중복되거나 매칭이 실패하면
조용히 틀린 분할을 쓰는 대신 멈춘다 — 분할이 틀리면 test 봉인이 깨진다.
"""
import csv
import os
import re


class JoinError(RuntimeError):
    """조인이 유일하지 않을 때. 조용히 넘기면 test 가 오염된다."""


def read_raw(path):
    """원본 CSV 를 dict 리스트로 읽는다. 값은 문자열 그대로 둔다."""
    with open(path, encoding='utf-8-sig', newline='') as f:
        return list(csv.DictReader(f))


def read_mapping(md_path):
    """`학습데이터셋-컬럼정의.md` 에서 {영문컬럼: 원문항} 을 뽑는다.

    표 형식: | `feature` | 한글라벨 | G6 | ...
    """
    pat = re.compile(r'^\| `([a-z_]+)` \| *([^|]+?) *\| *([A-Za-z0-9_]+) *\|', re.M)
    with open(md_path, encoding='utf-8') as f:
        found = {a: c for a, _, c in pat.findall(f.read())}
    found.pop('care_burden', None)
    return found


def _key(values):
    """조인 키. None·빈 문자열을 하나로 통일한다."""
    out = []
    for v in values:
        if v is None or (isinstance(v, str) and v.strip() == ''):
            out.append(None)
        else:
            out.append(str(v).strip())
    return tuple(out)


def join_split(raw_rows, db_rows, mapping):
    """raw 각 행에 `split`·`cv_fold` 를 붙여 새 dict 리스트를 돌려준다.

    mapping: {영문컬럼: 원문항}. target 은 care_burden ↔ I13 으로 고정.
    """
    feats = sorted(mapping)
    if db_rows:
        missing = [f for f in feats if f not in db_rows[0]]
        if missing:
            raise JoinError(f'DB 에 없는 컬럼이 매핑에 있다: {missing}')
    index = {}
    for r in db_rows:
        k = _key([r[f] for f in feats] + [r['care_burden']])
        if k in index:
            raise JoinError(f'DB 쪽에 값 튜플이 중복된다: {k}')
        index[k] = r

    out = []
    for row in raw_rows:
        k = _key([row[mapping[f]] for f in feats] + [row['I13']])
        hit = index.get(k)
        if hit is None:
            raise JoinError(f'매칭되는 DB 행이 없다 (ID={row.get("ID")}): {k}')
        merged = dict(row)
        merged['__split'] = hit['split']
        merged['__cv_fold'] = hit['cv_fold']
        out.append(merged)

    seen = len({_key([r[mapping[f]] for f in feats] + [r['I13']]) for r in raw_rows})
    if seen != len(raw_rows):
        raise JoinError(f'원본 쪽 값 튜플이 중복된다 ({seen} != {len(raw_rows)})')
    return out


def load_db_rows():
    """`cb_dataset_v1` 을 읽기 전용으로 조회한다. 쓰기는 하지 않는다."""
    import pymysql
    for line in open('.env', encoding='utf-8'):
        line = line.strip()
        if line and not line.startswith('#') and '=' in line:
            k, v = line.split('=', 1)
            os.environ.setdefault(k.strip(), v.strip().strip('\'"'))
    con = pymysql.connect(
        host=os.environ['DB_HOST'], port=int(os.environ['DB_PORT']),
        user=os.environ['DB_USER'], password=os.environ['DB_PASSWORD'],
        database=os.environ['DB_NAME'], charset='utf8mb4',
        cursorclass=pymysql.cursors.DictCursor)
    try:
        with con.cursor() as cur:
            cur.execute('SELECT * FROM cb_dataset_v1')
            return cur.fetchall()
    finally:
        con.close()
```

- [ ] **Step 5: 테스트가 통과하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: PASS 6건

- [ ] **Step 6: 실제 3,000행으로 조인 검증 게이트를 통과하는지 확인**

스펙 S1 의 게이트다. **3,000/3,000 이 아니면 여기서 멈추고 설계를 다시 본다.**

Run:
```bash
cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -c "
from sjh_cluster import load
raw = load.read_raw('data/snapshots/2024.csv')
mp  = load.read_mapping('docs/학습데이터셋-컬럼정의.md')
db  = load.load_db_rows()
print('매핑', len(mp), '· raw', len(raw), '· db', len(db))
joined = load.join_split(raw, db, mp)
import collections
print('조인 성공', len(joined), collections.Counter(r['__split'] for r in joined))"
```
Expected: `매핑 38 · raw 3000 · db 3000` 다음 줄에 `조인 성공 3000 Counter({'train': 2398, 'test': 602})`

- [ ] **Step 7: 커밋**

```bash
git add ml/sjh_cluster/__init__.py ml/sjh_cluster/load.py ml/tests/test_sjh_cluster.py
git -c user.name="신지현" commit -m "feat(군집): 원본 CSV 를 값 튜플로 기존 train/test 분할에 조인한다

row_key 가 행 내용 MD5 라 원본 ID 와 대응하지 않는다. 38변수+target 값 튜플로
조인하고, 중복이나 미매칭이 하나라도 있으면 JoinError 로 멈춘다 — 분할이 틀리면
test 602건 봉인이 조용히 깨진다.

검증: 실측 3,000/3,000 조인, train 2,398 / test 602

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: 컬럼 정리 (S2)

버릴 컬럼을 판정한다. 집합들이 서로 겹치므로 **합집합**으로 다룬다.

**Files:**
- Create: `ml/sjh_cluster/clean.py`
- Modify: `ml/tests/test_sjh_cluster.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`ml/tests/test_sjh_cluster.py` 끝에 붙인다.

```python
from sjh_cluster import clean


def test_drop_전부결측_상수_식별자():
    rows = [
        {'ID': '1', 'ADDCODE': '11', 'WT': '1.0', 'I13': '2', 'ALLNA': '', 'CONST': '7', 'OK': '1'},
        {'ID': '2', 'ADDCODE': '12', 'WT': '2.0', 'I13': '3', 'ALLNA': '', 'CONST': '7', 'OK': '2'},
    ]
    keep, reasons = clean.select_columns(rows, min_respondents=1)

    assert keep == ['OK']
    assert reasons['ALLNA'] == '전부결측'
    assert reasons['CONST'] == '상수'
    assert reasons['ID'] == '식별자'
    assert reasons['ADDCODE'] == '식별자'
    assert reasons['WT'] == '조사가중치'
    assert reasons['I13'] == 'target'


def test_drop_주관식_텍스트():
    rows = [{'X_op': '기타 사유입니다', 'N': '1'}, {'X_op': '', 'N': '2'}]
    keep, reasons = clean.select_columns(rows, min_respondents=1)
    assert keep == ['N']
    assert reasons['X_op'] == '주관식'


def test_drop_응답자_적은_블록():
    rows = [{'RARE': '1' if i == 0 else '', 'OK': str(i)} for i in range(10)]
    keep, reasons = clean.select_columns(rows, min_respondents=5)
    assert 'RARE' not in keep
    assert reasons['RARE'] == '응답자 1명 < 5'
    assert 'OK' in keep
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: FAIL — `No module named 'sjh_cluster.clean'`

- [ ] **Step 3: `clean.py` 구현**

```python
# -*- coding: utf-8 -*-
"""군집화에 쓸 컬럼을 고른다. 버리는 이유를 반드시 함께 돌려준다.

집합들이 서로 겹치므로(전부결측인 주관식 등) 판정 순서를 고정해 이유가
하나로 정해지게 한다.
"""

IDENTIFIERS = ('ID', 'ADDCODE')
WEIGHT = 'WT'
TARGET = 'I13'


def _blank(v):
    return v is None or str(v).strip() == ''


def select_columns(rows, min_respondents=100):
    """(남길 컬럼 목록, {버린 컬럼: 이유}) 를 돌려준다.

    min_respondents: 응답자가 이보다 적은 컬럼은 버린다. 거리 계산에 거의
    기여하지 못하면서 잡음만 넣는다.
    """
    cols = list(rows[0].keys()) if rows else []
    keep, reasons = [], {}

    for c in cols:
        vals = [r.get(c) for r in rows]
        answered = [v for v in vals if not _blank(v)]

        if c in IDENTIFIERS:
            reasons[c] = '식별자'
        elif c == WEIGHT:
            reasons[c] = '조사가중치'
        elif c == TARGET:
            reasons[c] = 'target'
        elif c.startswith('__'):
            reasons[c] = '조인메타'
        elif not answered:
            reasons[c] = '전부결측'
        elif c.endswith('_op'):
            reasons[c] = '주관식'
        elif len(answered) < min_respondents:
            # 응답자 수를 상수 검사보다 먼저 본다. 1명만 답한 컬럼은 자동으로
            # 상수가 되는데, '상수'라고 적으면 왜 버렸는지 오해를 부른다.
            reasons[c] = f'응답자 {len(answered)}명 < {min_respondents}'
        elif len(set(answered)) <= 1:
            reasons[c] = '상수'
        else:
            keep.append(c)

    return keep, reasons
```

- [ ] **Step 4: 테스트가 통과하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: PASS 9건

- [ ] **Step 5: 커밋**

```bash
git add ml/sjh_cluster/clean.py ml/tests/test_sjh_cluster.py
git -c user.name="신지현" commit -m "feat(군집): 버릴 컬럼을 이유와 함께 판정한다

전부결측·상수·식별자·가중치·target·주관식·소수응답 블록을 버린다. 집합이 서로
겹치므로 판정 순서를 고정해 이유가 하나로 정해지게 했다. 이유를 함께 돌려주는
이유는 최종 문서에 제외 근거를 표로 남기기 위해서다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: 게이트 추출 (S3)

**이 실험의 핵심 아이디어다.** 결측 패턴이 같은 컬럼은 같은 분기에 걸린 것이므로, 그 묶음마다 이진 변수 하나를 만든다.

**Files:**
- Create: `ml/sjh_cluster/gates.py`
- Modify: `ml/tests/test_sjh_cluster.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
from sjh_cluster import gates


def test_같은_결측패턴_컬럼은_게이트_하나로():
    # 행 1,2 는 E 블록에 응답, 행 0,3 은 미응답
    rows = [
        {'E1': '',  'E2': '',  'A1': '1'},
        {'E1': '5', 'E2': '3', 'A1': '2'},
        {'E1': '4', 'E2': '2', 'A1': '1'},
        {'E1': '',  'E2': '',  'A1': '2'},
    ]
    found = gates.find_gates(rows, ['E1', 'E2', 'A1'], min_columns=2)

    assert len(found) == 1
    g = found[0]
    assert sorted(g['columns']) == ['E1', 'E2']
    assert g['values'] == [0, 1, 1, 0]
    assert g['n_answered'] == 2


def test_결측이_없으면_게이트가_없다():
    rows = [{'A1': '1'}, {'A1': '2'}]
    assert gates.find_gates(rows, ['A1'], min_columns=2) == []


def test_혼자인_결측패턴은_게이트가_아니다():
    """min_columns 미만이면 분기가 아니라 개별 무응답으로 본다."""
    rows = [{'A1': '', 'B1': '1'}, {'A1': '2', 'B1': '2'}]
    assert gates.find_gates(rows, ['A1', 'B1'], min_columns=2) == []
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: FAIL — `No module named 'sjh_cluster.gates'`

- [ ] **Step 3: `gates.py` 구현**

```python
# -*- coding: utf-8 -*-
"""분기(skip logic) 구조를 결측 패턴에서 복원한다.

이 조사의 결측은 무응답이 아니라 분기다. A 문항에 '해당없음'을 답하면 B 블록
전체를 건너뛴다. 그래서 **같은 게이트에 걸린 컬럼은 결측 행 집합이 정확히 같다.**
코드북 없이 데이터만으로 구조가 나오는 이유다.

게이트를 이진 변수 하나로 승격시키는 이유 — 분기 하나가 컬럼 수십 개를 동시에
비우므로, 그대로 거리를 재면 "취업 경험 없음"이라는 사실 하나가 수십 표를 갖는다.
"""
import collections


def _blank(v):
    return v is None or str(v).strip() == ''


def find_gates(rows, columns, min_columns=2):
    """결측 패턴이 같은 컬럼을 묶어 게이트 목록을 만든다.

    돌려주는 각 항목:
      columns    이 게이트에 걸린 컬럼 이름들
      values     행마다 1(응답함) / 0(건너뜀)
      n_answered 응답한 행 수
    """
    by_pattern = collections.defaultdict(list)
    for c in columns:
        mask = tuple(_blank(r.get(c)) for r in rows)
        if any(mask):
            by_pattern[mask].append(c)

    found = []
    for mask, cols in by_pattern.items():
        if len(cols) < min_columns:
            continue
        values = [0 if m else 1 for m in mask]
        found.append({'columns': sorted(cols),
                      'values': values,
                      'n_answered': sum(values)})
    found.sort(key=lambda g: (-len(g['columns']), g['columns'][0]))
    return found


def gate_matrix(found):
    """게이트 값들을 (행, 게이트) 형태의 리스트로 바꾼다."""
    if not found:
        return []
    n = len(found[0]['values'])
    return [[g['values'][i] for g in found] for i in range(n)]
```

- [ ] **Step 4: 테스트가 통과하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: PASS 12건

- [ ] **Step 5: 커밋**

```bash
git add ml/sjh_cluster/gates.py ml/tests/test_sjh_cluster.py
git -c user.name="신지현" commit -m "feat(군집): 결측 패턴에서 설문 분기 구조를 복원한다

같은 게이트에 걸린 컬럼은 결측 행 집합이 정확히 같다. 이 성질로 코드북 없이
분기 구조가 나온다. 실측으로 507컬럼이 178가지 패턴으로 정리되고 338개가
~20개 게이트로 묶이는 것을 확인했다.

게이트를 이진 변수로 승격시키는 이유 — 분기 하나가 컬럼 수십 개를 동시에 비우므로
그대로 거리를 재면 한 사실이 수십 표를 갖는다. 어제 군집화가 부담이 아니라
취업 응답 여부로 갈린 원인이 이것이다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: 척도 판정 (S4)

코드북을 기계로 못 읽으므로 데이터에서 척도를 추정한다. **애매하면 nominal 로 보수적으로** 간다 — 순서가 아닌 것을 순서로 다루면 거리가 왜곡되지만, 순서를 nominal 로 다루면 정보를 조금 잃을 뿐이다.

**Files:**
- Create: `ml/sjh_cluster/scales.py`
- Modify: `ml/tests/test_sjh_cluster.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
from sjh_cluster import scales


def test_이진은_binary():
    vals = ['1', '2', '1', '2']
    assert scales.judge('A2', vals) == 'binary'


def test_연속형은_continuous():
    vals = [str(v) for v in range(20, 90)]      # 고유값 70개
    assert scales.judge('I9_2', vals) == 'continuous'


def test_작은_정수_연속범위는_ordinal():
    vals = ['1', '2', '3', '4', '5', '3', '2']
    assert scales.judge('G6', vals) == 'ordinal'


def test_구멍이_있으면_nominal():
    """1,2,3,9 처럼 코드가 띄엄띄엄하면 순서 척도로 보지 않는다."""
    vals = ['1', '2', '3', '9']
    assert scales.judge('I8_2', vals) == 'nominal'


def test_숫자가_아니면_nominal():
    assert scales.judge('D13', ['가', '나', '다']) == 'nominal'


def test_판정표를_한번에():
    rows = [{'A2': '1', 'G6': '1'}, {'A2': '2', 'G6': '5'}, {'A2': '1', 'G6': '3'}]
    table = scales.judge_all(rows, ['A2', 'G6'])
    assert table == {'A2': 'binary', 'G6': 'ordinal'}
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: FAIL — `No module named 'sjh_cluster.scales'`

- [ ] **Step 3: `scales.py` 구현**

```python
# -*- coding: utf-8 -*-
"""컬럼의 척도를 데이터에서 추정한다.

코드북 PDF 를 기계로 읽을 도구가 없어서 쓰는 대체 수단이다. 판정 규칙을 단순하고
설명 가능하게 두고, **애매하면 nominal 로 보수적으로** 간다 — 순서가 아닌 것을
순서로 다루면 거리가 왜곡되지만, 순서를 nominal 로 다루면 정보를 조금 잃을 뿐이다.

판정 결과는 문서에 표로 남겨 사람이 검토한다.
"""

CONTINUOUS_MIN_LEVELS = 15


def _blank(v):
    return v is None or str(v).strip() == ''


def judge(name, values):
    """한 컬럼의 척도를 'binary' / 'ordinal' / 'nominal' / 'continuous' 로 판정한다."""
    answered = [str(v).strip() for v in values if not _blank(v)]
    if not answered:
        return 'nominal'

    try:
        nums = [float(v) for v in answered]
    except ValueError:
        return 'nominal'                      # 주관식·문자 코드

    levels = sorted(set(nums))
    if len(levels) <= 2:
        return 'binary'
    if len(levels) >= CONTINUOUS_MIN_LEVELS:
        return 'continuous'
    if any(v != int(v) for v in levels):
        return 'continuous'                   # 소수점이 있으면 연속형

    # 정수 코드가 구멍 없이 이어지면 순서 척도로 본다 (1,2,3,4,5).
    # 1,2,3,9 처럼 띄엄띄엄하면 '9=기타' 같은 코드일 가능성이 높으므로 nominal.
    ints = [int(v) for v in levels]
    if ints == list(range(ints[0], ints[0] + len(ints))):
        return 'ordinal'
    return 'nominal'


def judge_all(rows, columns):
    """{컬럼: 척도} 판정표를 만든다."""
    return {c: judge(c, [r.get(c) for r in rows]) for c in columns}
```

- [ ] **Step 4: 테스트가 통과하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: PASS 18건

- [ ] **Step 5: 커밋**

```bash
git add ml/sjh_cluster/scales.py ml/tests/test_sjh_cluster.py
git -c user.name="신지현" commit -m "feat(군집): 척도를 데이터에서 추정한다 — 애매하면 nominal

코드북 PDF 를 기계로 읽을 도구가 없어 쓰는 대체 수단이다. 레벨 2개는 binary,
15개 이상이거나 소수점이 있으면 continuous, 정수 코드가 구멍 없이 이어지면
ordinal, 나머지는 nominal 로 본다.

보수적으로 간 이유 — 순서가 아닌 것을 순서로 다루면 거리가 왜곡되지만,
순서를 nominal 로 다루면 정보를 조금 잃을 뿐이다. 1,2,3,9 처럼 띄엄띄엄한
코드는 9=기타 일 가능성이 높으므로 nominal 로 둔다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: Gower 거리 (S5)

**가장 조심해야 할 부분이다.** 직접 구현하므로 정확성을 테스트로 못 박는다.

**Files:**
- Create: `ml/sjh_cluster/gower.py`
- Modify: `ml/tests/test_sjh_cluster.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
from sjh_cluster import gower


def test_같은_행은_거리0():
    X = np.array([[1.0, 3.0], [1.0, 3.0]])
    D = gower.distance(X, ['binary', 'ordinal'])
    assert D.shape == (2, 2)
    assert D[0, 1] == pytest.approx(0.0)
    assert D[0, 0] == pytest.approx(0.0)


def test_대칭이다():
    X = np.array([[1.0, 3.0], [2.0, 5.0], [1.0, 1.0]])
    D = gower.distance(X, ['nominal', 'ordinal'])
    assert np.allclose(D, D.T)


def test_이진은_일치_불일치():
    X = np.array([[1.0], [2.0]])
    D = gower.distance(X, ['binary'])
    assert D[0, 1] == pytest.approx(1.0)


def test_순서형은_범위로_정규화():
    """1~5 척도에서 1 과 3 의 거리는 (3-1)/(5-1) = 0.5."""
    X = np.array([[1.0], [3.0], [5.0]])
    D = gower.distance(X, ['ordinal'])
    assert D[0, 1] == pytest.approx(0.5)
    assert D[0, 2] == pytest.approx(1.0)


def test_결측은_쌍에서_제외된다():
    """두 변수 중 하나가 결측이면 나머지 하나로만 잰다."""
    X = np.array([[1.0, 1.0], [1.0, np.nan]])
    D = gower.distance(X, ['binary', 'binary'])
    assert D[0, 1] == pytest.approx(0.0)      # 비교 가능한 변수는 값이 같다


def test_모두_결측이면_거리1():
    """비교할 변수가 하나도 없으면 최대 거리로 둔다 — 임의로 가깝다고 하지 않는다."""
    X = np.array([[np.nan], [np.nan]])
    D = gower.distance(X, ['binary'])
    assert D[0, 1] == pytest.approx(1.0)


def test_가중치가_반영된다():
    """두 번째 변수에 가중치 3 을 주면 그쪽 불일치가 3배로 반영된다."""
    X = np.array([[1.0, 1.0], [2.0, 2.0]])
    D_even = gower.distance(X, ['binary', 'binary'], weights=[1.0, 1.0])
    D_tilt = gower.distance(X, ['binary', 'binary'], weights=[1.0, 3.0])
    assert D_even[0, 1] == pytest.approx(1.0)
    assert D_tilt[0, 1] == pytest.approx(1.0)   # 둘 다 불일치면 여전히 1

    Y = np.array([[1.0, 1.0], [1.0, 2.0]])      # 두 번째만 불일치
    assert gower.distance(Y, ['binary', 'binary'], [1.0, 1.0])[0, 1] == pytest.approx(0.5)
    assert gower.distance(Y, ['binary', 'binary'], [1.0, 3.0])[0, 1] == pytest.approx(0.75)


def test_블록_가중치는_문항수로_나눈다():
    """같은 게이트에 딸린 문항 3개는 각각 1/3 의 표를 갖는다."""
    w = gower.block_weights(['A', 'B', 'C', 'D'], {'A': 'g1', 'B': 'g1', 'C': 'g1'})
    assert w == pytest.approx([1 / 3, 1 / 3, 1 / 3, 1.0])
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: FAIL — `No module named 'sjh_cluster.gower'`

- [ ] **Step 3: `gower.py` 구현**

```python
# -*- coding: utf-8 -*-
"""Gower 거리 — 혼합형 데이터를 위한 거리.

왜 유클리드가 아닌가 — 이 데이터는 binary·nominal·ordinal·continuous 가 섞여 있다.
전부 원-핫으로 펴서 유클리드를 쓰면 순서형의 순서가 사라지고, 레벨 수가 많은 변수가
총 분산을 지배한다. Gower 는 변수 유형별로 다른 거리를 쓰고 각각 [0,1] 로 정규화한
뒤 가중 평균한다.

    d(i,j) = Σ w_k · d_k(i,j) / Σ w_k        (둘 다 응답한 변수 k 에 대해서만)

결측은 **쌍 단위로 제외**한다(pairwise deletion). 분기 결측은 "해당 없음"이므로,
비교할 수 없는 문항을 억지로 0 이나 1 로 채우지 않는다.

메모리 — n=2,398 이면 (n,n) float32 가 23MB. 변수마다 임시 행렬 하나를 쓰므로
동시 사용량은 100MB 정도다.
"""
import numpy as np


def block_weights(columns, column_block):
    """같은 블록에 딸린 문항들이 합쳐서 1표가 되도록 가중치를 만든다.

    column_block: {컬럼: 블록이름}. 블록이 없는 컬럼은 1.0.

    왜 필요한가 — 취업 블록 52문항을 각각 1표로 두면 "취업 경험 없음"이라는 사실
    하나가 52표를 갖는다. 블록당 기여를 같게 맞춘다.
    """
    counts = {}
    for c in columns:
        b = column_block.get(c)
        if b is not None:
            counts[b] = counts.get(b, 0) + 1
    return [1.0 / counts[column_block[c]] if column_block.get(c) else 1.0
            for c in columns]


def distance(X, kinds, weights=None):
    """(n, p) 실수 배열에서 (n, n) Gower 거리 행렬을 만든다.

    X      결측은 np.nan. nominal·binary 도 숫자 코드로 넣는다.
    kinds  각 열의 척도 — 'binary' | 'nominal' | 'ordinal' | 'continuous'
    """
    X = np.asarray(X, dtype=np.float64)
    n, p = X.shape
    if weights is None:
        weights = [1.0] * p
    if len(kinds) != p or len(weights) != p:
        raise ValueError(f'열 수와 맞지 않는다: p={p} kinds={len(kinds)} w={len(weights)}')

    num = np.zeros((n, n), dtype=np.float32)
    den = np.zeros((n, n), dtype=np.float32)

    for k in range(p):
        x = X[:, k]
        ok = ~np.isnan(x)
        pair = (ok[:, None] & ok[None, :])
        if not pair.any():
            continue

        if kinds[k] in ('ordinal', 'continuous'):
            lo, hi = np.nanmin(x), np.nanmax(x)
            rng = hi - lo
            if rng <= 0:
                d = np.zeros((n, n), dtype=np.float32)
            else:
                xf = np.where(ok, x, 0.0)
                d = (np.abs(xf[:, None] - xf[None, :]) / rng).astype(np.float32)
        else:                                   # binary · nominal
            xf = np.where(ok, x, 0.0)
            d = (xf[:, None] != xf[None, :]).astype(np.float32)

        w = np.float32(weights[k])
        num += w * np.where(pair, d, np.float32(0.0))
        den += w * pair.astype(np.float32)

    # 비교 가능한 변수가 하나도 없는 쌍은 최대 거리로 둔다.
    # 임의로 0(가깝다)으로 두면 군집이 결측 때문에 뭉친다.
    out = np.ones((n, n), dtype=np.float32)
    has = den > 0
    np.divide(num, den, out=out, where=has)
    np.fill_diagonal(out, 0.0)
    return np.clip((out + out.T) / 2.0, 0.0, 1.0)
```

- [ ] **Step 4: 테스트가 통과하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: PASS 26건

- [ ] **Step 5: 커밋**

```bash
git add ml/sjh_cluster/gower.py ml/tests/test_sjh_cluster.py
git -c user.name="신지현" commit -m "feat(군집): Gower 거리 직접 구현 — 결측 쌍 제외와 블록 가중치

혼합형 데이터에 유클리드를 쓰려면 전부 원-핫으로 펴야 하는데, 그러면 순서형의
순서가 사라지고 레벨 수가 많은 변수가 총 분산을 지배한다. 변수 유형별로 다른
거리를 쓰고 [0,1] 로 정규화한 뒤 가중 평균한다.

두 가지 판단을 코드에 박았다.
- 분기 결측은 '해당 없음'이므로 쌍 단위로 제외한다. 억지로 채우지 않는다
- 비교 가능한 변수가 하나도 없는 쌍은 거리 1(최대)로 둔다. 0 으로 두면
  군집이 결측 때문에 뭉친다
- 블록 가중치 — 취업 52문항을 각각 1표로 두면 한 사실이 52표를 갖는다

검증: 대칭성·대각0·순서형 정규화·결측 제외·가중치 반영 8건

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: 군집화 (S6)

거리 행렬만 받아 군집한다. `sklearn_extra` 가 없으므로 **PAM 을 직접 구현**하고, 계층군집은 scipy 를 쓴다.

**Files:**
- Create: `ml/sjh_cluster/cluster.py`
- Modify: `ml/tests/test_sjh_cluster.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
from sjh_cluster import cluster


def _two_blobs():
    """0,1 은 서로 가깝고 2,3 도 서로 가깝다. 두 덩어리 사이는 멀다."""
    return np.array([
        [0.0, 0.1, 0.9, 0.9],
        [0.1, 0.0, 0.9, 0.9],
        [0.9, 0.9, 0.0, 0.1],
        [0.9, 0.9, 0.1, 0.0],
    ])


def test_pam_두덩어리를_찾는다():
    labels = cluster.pam(_two_blobs(), k=2, seed=1)
    assert labels[0] == labels[1]
    assert labels[2] == labels[3]
    assert labels[0] != labels[2]


def test_pam_같은_시드는_같은_결과():
    D = _two_blobs()
    assert np.array_equal(cluster.pam(D, k=2, seed=7), cluster.pam(D, k=2, seed=7))


def test_pam_라벨은_0부터_k미만():
    labels = cluster.pam(_two_blobs(), k=2, seed=1)
    assert set(labels) == {0, 1}


def test_계층군집도_두덩어리를_찾는다():
    labels = cluster.hierarchical(_two_blobs(), k=2)
    assert labels[0] == labels[1]
    assert labels[2] == labels[3]
    assert labels[0] != labels[2]
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: FAIL — `No module named 'sjh_cluster.cluster'`

- [ ] **Step 3: `cluster.py` 구현**

```python
# -*- coding: utf-8 -*-
"""거리 행렬 위에서 군집화한다.

KMeans 를 쓰지 않는 이유 — 평균이 정의되지 않는다. 범주형이 섞인 데이터에서
"성별의 평균"은 뜻이 없다. 대신 실제 사람 하나를 중심으로 삼는 k-medoids(PAM)와
거리 행렬만으로 도는 계층군집을 쓴다.

sklearn_extra 가 설치돼 있지 않아 PAM 은 직접 구현한다(Park & Jun 의 교대 갱신).
"""
import numpy as np
from scipy.cluster.hierarchy import fcluster, linkage
from scipy.spatial.distance import squareform


def pam(D, k, seed=0, max_iter=100):
    """k-medoids. 각 군집의 중심은 실제 데이터 점(medoid)이다."""
    D = np.asarray(D, dtype=np.float64)
    n = len(D)
    if k > n:
        raise ValueError(f'k={k} 가 표본 수 {n} 보다 크다')

    # 초기 medoid — 거리 합이 작은 점부터 고르면 시드에 덜 흔들린다(Park & Jun).
    rng = np.random.default_rng(seed)
    order = np.argsort(D.sum(axis=1))
    medoids = list(order[:k])

    labels = np.zeros(n, dtype=int)
    for _ in range(max_iter):
        labels = np.argmin(D[:, medoids], axis=1)

        moved = False
        for c in range(k):
            members = np.flatnonzero(labels == c)
            if len(members) == 0:
                # 빈 군집 — 가장 먼 점을 새 medoid 로 준다
                far = int(np.argmax(np.min(D[:, medoids], axis=1)))
                if far != medoids[c]:
                    medoids[c] = far
                    moved = True
                continue
            within = D[np.ix_(members, members)].sum(axis=1)
            best = int(members[np.argmin(within)])
            if best != medoids[c]:
                medoids[c] = best
                moved = True
        if not moved:
            break

    return np.argmin(D[:, medoids], axis=1)


def hierarchical(D, k, method='average'):
    """계층군집. average linkage 는 거리 행렬만 있으면 되고 이상치에 둔감하다."""
    D = np.asarray(D, dtype=np.float64)
    np.fill_diagonal(D, 0.0)
    Z = linkage(squareform(D, checks=False), method=method)
    return fcluster(Z, t=k, criterion='maxclust') - 1
```

- [ ] **Step 4: 테스트가 통과하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: PASS 30건

- [ ] **Step 5: 커밋**

```bash
git add ml/sjh_cluster/cluster.py ml/tests/test_sjh_cluster.py
git -c user.name="신지현" commit -m "feat(군집): PAM 직접 구현과 계층군집

KMeans 를 쓰지 않는 이유 — 평균이 정의되지 않는다. 범주형이 섞인 데이터에서
'성별의 평균'은 뜻이 없다. 실제 사람 하나를 중심으로 삼는 k-medoids 와 거리
행렬만으로 도는 계층군집을 쓴다.

sklearn_extra 가 없어 PAM 은 Park & Jun 의 교대 갱신으로 직접 구현했다.
초기 medoid 를 거리 합이 작은 점부터 고르면 시드에 덜 흔들린다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: 채점 (§6)

**이 실험의 본체다.** 실루엣 하나로 판정하지 않고 네 가지를 함께 본다.

**Files:**
- Create: `ml/sjh_cluster/score.py`
- Modify: `ml/tests/test_sjh_cluster.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
from sjh_cluster import score


def test_고부담_리프트():
    """군집 0 은 전원 고부담(1,2), 군집 1 은 전원 저부담."""
    y = np.array([1, 2, 1, 4, 5, 3])
    labels = np.array([0, 0, 0, 1, 1, 1])
    lift = score.high_burden_lift(labels, y)

    assert lift[0]['n'] == 3
    assert lift[0]['high_ratio'] == pytest.approx(1.0)
    assert lift[0]['lift'] == pytest.approx(2.0)      # 전체 고부담 비율 0.5
    assert lift[1]['high_ratio'] == pytest.approx(0.0)


def test_게이트_오염도_완전일치면_1():
    labels = np.array([0, 0, 1, 1])
    gate_rows = [[1, 0], [1, 0], [0, 1], [0, 1]]
    assert score.gate_contamination(labels, gate_rows) == pytest.approx(1.0)


def test_게이트_오염도_무관하면_0에_가깝다():
    labels = np.array([0, 1, 0, 1])
    gate_rows = [[1], [1], [1], [1]]              # 게이트가 상수면 정보가 없다
    assert score.gate_contamination(labels, gate_rows) == pytest.approx(0.0)


def test_교차표():
    """군집 × 부담구간 교차표. 스펙 §6 이 ARI 와 함께 보라고 한 것."""
    y = np.array([1, 2, 3, 1, 5])
    labels = np.array([0, 0, 0, 1, 1])
    ct = score.crosstab(labels, y)
    assert ct[0] == {1: 1, 2: 1, 3: 1, 4: 0, 5: 0}
    assert ct[1] == {1: 1, 2: 0, 3: 0, 4: 0, 5: 1}


def test_널판정_상위5퍼센트_밖이면_구조있음():
    verdict = score.null_verdict(observed=0.30, null_scores=[0.10] * 20)
    assert verdict['structured'] is True
    assert verdict['p_value'] == pytest.approx(0.0)


def test_널판정_널분포와_겹치면_구조없음():
    verdict = score.null_verdict(observed=0.11, null_scores=[0.10, 0.12, 0.11, 0.13])
    assert verdict['structured'] is False
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: FAIL — `No module named 'sjh_cluster.score'`

- [ ] **Step 3: `score.py` 구현**

```python
# -*- coding: utf-8 -*-
"""군집을 채점한다.

지표 하나로 단정하지 않는다. 앞선 실험은 ARI 0.027 만 보고 '부담과 무관'이라고
정리했는데, 같은 교차표에서 한 군집은 고부담 80.6%(전체 54.4% 대비 1.48배)였다.
ARI 는 '5개 라벨이 통째로 일치하는가'를 재는 엄격한 지표라 부분적인 신호를 지운다.
그래서 리프트를 함께 본다.
"""
import numpy as np
from sklearn.metrics import (adjusted_rand_score, normalized_mutual_info_score,
                             silhouette_score)

HIGH_BURDEN = (1, 2)          # 1 최고부담군 · 2 고부담군 (역방향 척도)


def silhouette(D, labels):
    """거리 행렬 위에서 실루엣을 잰다. 군집이 하나뿐이면 정의되지 않는다."""
    if len(set(labels)) < 2:
        return float('nan')
    return float(silhouette_score(np.asarray(D, dtype=np.float64), labels,
                                  metric='precomputed'))


def agreement(labels, y):
    """군집이 부담 구간을 재현하는 정도."""
    return {'ari': float(adjusted_rand_score(y, labels)),
            'nmi': float(normalized_mutual_info_score(y, labels))}


def high_burden_lift(labels, y):
    """군집별 고부담 비율과 전체 대비 리프트.

    ARI 가 0 근처여도 특정 군집에 고부담이 몰려 있으면 그것은 실재하는 신호다.
    """
    y = np.asarray(y)
    base = float(np.isin(y, HIGH_BURDEN).mean())
    out = {}
    for c in sorted(set(labels)):
        m = np.asarray(labels) == c
        ratio = float(np.isin(y[m], HIGH_BURDEN).mean())
        out[int(c)] = {'n': int(m.sum()),
                       'high_ratio': ratio,
                       'lift': ratio / base if base else float('nan')}
    return out


def crosstab(labels, y):
    """군집 × 부담구간 교차표. {군집: {구간: 인원}}.

    ARI 는 5개 라벨이 통째로 일치하는가만 재므로 부분 신호를 지운다. 교차표를
    함께 봐야 '어느 군집에 고부담이 몰렸는가'가 보인다.
    """
    y = np.asarray(y)
    labels = np.asarray(labels)
    return {int(c): {lv: int(((labels == c) & (y == lv)).sum()) for lv in (1, 2, 3, 4, 5)}
            for c in sorted(set(labels.tolist()))}


def gate_contamination(labels, gate_rows):
    """군집이 설문 분기로 갈렸는지. 1 에 가까우면 '분기 군집'이다."""
    ids = [hash(tuple(r)) for r in gate_rows]
    if len(set(ids)) < 2:
        return 0.0
    return float(normalized_mutual_info_score(ids, labels))


def null_verdict(observed, null_scores, alpha=0.05):
    """실측 실루엣이 널 분포의 상위 alpha 밖인지 판정한다.

    이것이 없으면 '구조가 없다'와 '방법이 구조를 못 본다'를 구분할 수 없다.
    """
    null = np.asarray(null_scores, dtype=float)
    p = float((null >= observed).mean())
    return {'observed': float(observed),
            'null_mean': float(null.mean()),
            'null_max': float(null.max()),
            'p_value': p,
            'structured': bool(p < alpha)}
```

- [ ] **Step 4: 테스트가 통과하는지 확인**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: PASS 36건

- [ ] **Step 5: 커밋**

```bash
git add ml/sjh_cluster/score.py ml/tests/test_sjh_cluster.py
git -c user.name="신지현" commit -m "feat(군집): 채점 — 널 판정·게이트 오염도·고부담 리프트

지표 하나로 단정하지 않는다. 앞선 실험은 ARI 0.027 만 보고 '부담과 무관'이라고
정리했는데, 같은 교차표에서 한 군집은 고부담 80.6%(전체 54.4% 대비 1.48배)였다.
ARI 는 5개 라벨이 통째로 일치하는가를 재는 엄격한 지표라 부분 신호를 지운다.

널 판정이 이 실험의 본체다. 이것이 없으면 '구조가 없다'와 '방법이 구조를
못 본다'를 구분할 수 없다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: 실행 엔트리와 전체 실행

모듈을 엮어 A안을 끝까지 돌린다. 누수 6변수 **포함/제외 두 벌**을 같은 방법으로 돌린다.

**Files:**
- Create: `ml/SJH-cluster-raw.py`

- [ ] **Step 1: 엔트리 스크립트를 쓴다**

```python
# -*- coding: utf-8 -*-
"""원본 507컬럼 군집화 — A안 (읽기 전용 실험).

    PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 ml/SJH-cluster-raw.py \
        --null-reps 20 --out docs/SJH-군집화-결과.json

하는 일 — 원본 CSV 에 기존 train/test 분할을 붙이고(test 는 열지 않는다),
분기 결측을 게이트 이진 변수로 승격시킨 뒤, Gower 거리로 PAM·계층군집을 돌리고,
열별 셔플 널 베이스라인과 비교해 구조 유무를 판정한다.

DB 는 조회만 하고 models/ 는 건드리지 않는다.
"""
import argparse
import json
import sys
from datetime import datetime

import numpy as np

from sjh_cluster import clean, cluster, gates, gower, load, scales, score

# 누수 6변수의 원본 컬럼 (docs/2024csv_column_mapping.xlsx 대조, FR-004b)
LEAK_COLUMNS = {
    'I14':   'caregiver_life_satisfaction',
    'I10':   'care_difficulty_top1',
    'I12_1': 'needed_care_service_type',
    'I11_H': 'work_care_gap_hours',
    'I11':   'work_care_gap_exp',
    'I12':   'integrated_care_awareness',
}
K_RANGE = range(2, 11)
SEED = 20260902


def build_matrix(rows, keep, found_gates):
    """(X, kinds, 컬럼이름) 을 만든다.

    A안의 입력 = 전원응답 컬럼 + 게이트 이진 변수.
    게이트는 나머지 컬럼의 '존재 여부'를 압축한 것이라, 내용을 안 써도
    507컬럼의 구조 정보가 반영된다.
    """
    full = [c for c in keep if all(str(r.get(c, '')).strip() != '' for r in rows)]
    kinds_map = scales.judge_all(rows, full)

    codes = {}
    for c in full:
        if kinds_map[c] in ('ordinal', 'continuous'):
            codes[c] = [float(str(r[c]).strip()) for r in rows]
        else:
            levels = {v: i for i, v in enumerate(sorted({str(r[c]).strip() for r in rows}))}
            codes[c] = [float(levels[str(r[c]).strip()]) for r in rows]

    names = list(full)
    kinds = [kinds_map[c] for c in full]
    cols = [codes[c] for c in full]

    for i, g in enumerate(found_gates):
        names.append(f'__gate{i}')
        kinds.append('binary')
        cols.append([float(v) for v in g['values']])

    X = np.array(cols, dtype=np.float64).T
    return X, kinds, names


def run_once(X, kinds, weights, y, gate_rows, null_reps, rng):
    """한 조건(누수 포함 또는 제외)을 끝까지 돌린다."""
    D = gower.distance(X, kinds, weights)
    rows = []
    for k in K_RANGE:
        for name, labels in (('pam', cluster.pam(D, k, seed=SEED)),
                             ('hier', cluster.hierarchical(D, k))):
            rec = {'k': k, 'method': name,
                   'silhouette': score.silhouette(D, labels),
                   'gate_contamination': score.gate_contamination(labels, gate_rows)}
            rec.update(score.agreement(labels, y))
            rec['lift'] = score.high_burden_lift(labels, y)
            rec['crosstab'] = score.crosstab(labels, y)
            rows.append(rec)

    best = max((r for r in rows if r['method'] == 'pam'), key=lambda r: r['silhouette'])

    nulls = []
    for i in range(null_reps):
        Xs = X.copy()
        for c in range(Xs.shape[1]):
            Xs[:, c] = rng.permutation(Xs[:, c])
        Dn = gower.distance(Xs, kinds, weights)
        nulls.append(score.silhouette(Dn, cluster.pam(Dn, best['k'], seed=SEED)))
        print(f'  널 {i + 1}/{null_reps} 실루엣 {nulls[-1]:.4f}', file=sys.stderr)

    return {'rows': rows, 'best': best,
            'null': score.null_verdict(best['silhouette'], nulls)}


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument('--null-reps', type=int, default=20)
    ap.add_argument('--out', default='docs/SJH-군집화-결과.json')
    ap.add_argument('--csv', default='data/snapshots/2024.csv')
    ap.add_argument('--mapping', default='docs/학습데이터셋-컬럼정의.md')
    args = ap.parse_args()

    raw = load.read_raw(args.csv)
    mapping = load.read_mapping(args.mapping)
    joined = load.join_split(raw, load.load_db_rows(), mapping)

    train = [r for r in joined if r['__split'] == 'train']
    print(f'train {len(train)}건 (test 는 열지 않는다)', file=sys.stderr)
    y = np.array([int(r['I13']) for r in train])

    result = {'generated_at': datetime.now().isoformat(timespec='seconds'),
              'n_train': len(train), 'seed': SEED, 'null_reps': args.null_reps,
              'conditions': {}}
    rng = np.random.default_rng(SEED)

    for label, drop_leak in (('누수 포함', False), ('누수 제외', True)):
        rows = train
        keep, reasons = clean.select_columns(rows, min_respondents=100)
        if drop_leak:
            keep = [c for c in keep if c not in LEAK_COLUMNS]
        found = gates.find_gates(rows, keep, min_columns=2)
        X, kinds, names = build_matrix(rows, keep, found)

        # A안은 블록마다 게이트 변수가 이미 하나뿐이라 중복 계수가 없다.
        # 그래서 가중치는 전부 1.0 이다. gower.block_weights 는 게이트 아래
        # 문항의 '내용'까지 넣는 B안에서 쓰인다.
        weights = [1.0] * len(names)
        print(f'[{label}] 컬럼 {len(names)} · 게이트 {len(found)}', file=sys.stderr)

        out = run_once(X, kinds, weights, y, gates.gate_matrix(found), args.null_reps, rng)
        out['n_columns'] = len(names)
        out['n_gates'] = len(found)
        out['dropped'] = {c: r for c, r in sorted(reasons.items())}
        out['scales'] = {n: k for n, k in zip(names, kinds)}
        result['conditions'][label] = out

    with open(args.out, 'w', encoding='utf-8') as f:
        json.dump(result, f, ensure_ascii=False, indent=2)
    print(f'\n저장: {args.out}', file=sys.stderr)

    for label, c in result['conditions'].items():
        v = c['null']
        print(f'[{label}] 최고 k={c["best"]["k"]} 실루엣 {v["observed"]:.4f} · '
              f'널 평균 {v["null_mean"]:.4f} · p={v["p_value"]:.3f} · '
              f'구조 {"있음" if v["structured"] else "없음"}', file=sys.stderr)


if __name__ == '__main__':
    main()
```

- [ ] **Step 2: 널 1회로 빠르게 동작 확인**

전체 실행 전에 파이프라인이 끝까지 도는지부터 본다.

Run:
```bash
cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml \
  python3 ml/SJH-cluster-raw.py --null-reps 1 --out /tmp/smoke.json
```
Expected: `train 2398건 (test 는 열지 않는다)` 로 시작해 두 조건이 각각 `[누수 포함] 컬럼 <수> · 게이트 <수>` 를 찍고, 마지막에 조건별 요약 두 줄이 나온다. 오류 없이 끝나야 한다.

- [ ] **Step 3: 검증 게이트 — 결과가 말이 되는지 본다**

Run:
```bash
cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -c "
import json; d=json.load(open('/tmp/smoke.json'))
for k,c in d['conditions'].items():
    print(k, '컬럼', c['n_columns'], '게이트', c['n_gates'],
          '· 최고 실루엣', round(c['best']['silhouette'],4),
          '· 게이트오염', round(c['best']['gate_contamination'],4))"
```
Expected: 두 조건 모두 컬럼 수가 100~250 사이, 게이트 10~30개. **컬럼이 10개 미만이거나 게이트가 0개면 멈추고 `clean`·`gates` 판정을 다시 본다.**

- [ ] **Step 4: 본 실행 (널 20회)**

거리 행렬 계산이 조건당 20회 반복되므로 **20~40분** 걸린다.

Run:
```bash
cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml \
  python3 ml/SJH-cluster-raw.py --null-reps 20 --out docs/SJH-군집화-결과.json
```
Expected: 마지막에 조건별로 `구조 있음` 또는 `구조 없음` 이 찍힌다. **어느 쪽이 나와도 성공이다.**

- [ ] **Step 5: 커밋**

```bash
git add ml/SJH-cluster-raw.py docs/SJH-군집화-결과.json
git -c user.name="신지현" commit -m "feat(군집): A안 실행 엔트리와 실측 결과

전원응답 컬럼 + 게이트 이진 변수로 Gower 거리를 만들고 PAM·계층군집을 k=2~10 에서
돌린 뒤, 열별 셔플 널 베이스라인 20회와 비교해 구조 유무를 판정한다.
누수 6변수 포함/제외 두 벌을 같은 방법으로 돌린다.

train 2,398건만 쓰고 test 602건은 열지 않는다. DB 는 조회만 한다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: 결과 문서화

**Files:**
- Create: `docs/SJH-군집화-원본데이터.md`

- [ ] **Step 1: 결과 JSON 에서 표를 뽑는다**

Run:
```bash
cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -c "
import json; d=json.load(open('docs/SJH-군집화-결과.json'))
for name,c in d['conditions'].items():
    print('##', name, '— 컬럼', c['n_columns'], '게이트', c['n_gates'])
    print('| k | 방법 | 실루엣 | ARI | NMI | 게이트오염 |')
    print('|---|---|---|---|---|---|')
    for r in c['rows']:
        print(f\"| {r['k']} | {r['method']} | {r['silhouette']:.4f} | {r['ari']:.4f} | {r['nmi']:.4f} | {r['gate_contamination']:.4f} |\")
    v=c['null']
    print(f\"\n널 판정: 실측 {v['observed']:.4f} · 널 평균 {v['null_mean']:.4f} · 널 최대 {v['null_max']:.4f} · p={v['p_value']:.3f} → 구조 {'있음' if v['structured'] else '없음'}\n\")
    print('군집별 고부담 리프트:', {k: round(x['lift'],3) for k,x in c['best']['lift'].items()}, '\n')"
```
Expected: 조건별 표와 널 판정 줄이 출력된다. 이 출력을 문서에 붙인다.

- [ ] **Step 2: 문서를 쓴다**

`docs/SJH-군집화-원본데이터.md` 에 아래 목차로 쓴다. **Step 1 의 실제 출력값을 넣는다 — 추정치를 쓰지 않는다.**

```markdown
# 원본 507컬럼 군집화 — 방법과 결과

## 1. 한 줄 결론
(널 판정 결과를 한 문장으로. "구조가 있다" 또는 "구조가 없다"를 먼저 적는다)

## 2. 무엇을 어떻게 했나
- 입력: 원본 507컬럼 → 정리 후 <n_columns>개 (게이트 <n_gates>개 포함)
- 거리: Gower (블록 가중치 정규화, 결측 쌍 제외)
- 군집: PAM · 계층군집, k=2~10
- 판정: 열별 셔플 널 베이스라인 20회

## 3. 제외한 컬럼 (이유별)
(결과 JSON 의 `dropped` 를 이유별로 묶어 표로)

## 4. 게이트 목록
(게이트별 컬럼 수와 응답자 수)

## 5. 척도 판정표
(결과 JSON 의 `scales`. 코드북 없이 데이터로 추정했음을 명시)

## 6. 결과
(Step 1 의 표 그대로)

## 7. 널 베이스라인 판정
(실측 vs 널 분포. 이것이 이 실험의 본체임을 적는다)

## 8. 군집이 부담과 어떤 관계인가
(교차표와 고부담 리프트. ARI 하나로 단정하지 않는다)

## 9. 누수 포함 / 제외 비교
(두 조건의 차이. 군집이 I14 로 갈렸는지)

## 10. 한계
- 척도 판정을 코드북이 아니라 데이터 휴리스틱으로 했다
- A안은 게이트 아래 문항의 **내용**을 쓰지 않았다 (B안에서 다룬다)
- test 602건은 열지 않았다

## 11. 기존 실험(HG-실험-군집화)과의 차이
(방법 3건·해석 3건. 설계 문서 §1 참조)
```

- [ ] **Step 3: 커밋**

```bash
git add docs/SJH-군집화-원본데이터.md
git -c user.name="신지현" commit -m "docs(군집): 원본 507컬럼 군집화 방법과 결과

널 베이스라인 판정을 포함한 전체 결과. 이 판정이 있어야 '구조가 없다'와
'방법이 구조를 못 본다'를 구분할 수 있다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

- [ ] **Step 4: 전체 테스트를 한 번 더 돌린다**

Run: `cd ~/project2608-sjh && PYTHONPATH=ml/.pylibs-sjh:ml/.pylibs:ml python3 -m pytest ml/tests/test_sjh_cluster.py -v`
Expected: PASS 36건

- [ ] **Step 5: 작업 기록 갱신**

`~/project2608/docs/작업기록/신지현.md` 의 §14 표에 `HH:MM` 행을 **이어 붙인다**(덮어쓰지 않는다). 실행 결과와 널 판정 결론을 적는다.

---

## 다음 단계 (이 계획 밖)

A안 결과의 **게이트 오염도**를 보고 정한다.

| A안 결과 | 다음 |
|---|---|
| 게이트 오염도가 높다 (군집이 분기로 갈렸다) | **C안** — 2층 군집화로 처지 유형을 먼저 가르고 그 안에서 다시 |
| 게이트 오염도가 낮고 구조가 있다 | **B안** — 게이트 아래 문항의 내용까지 Gower 에 넣어 해상도를 올린다 |
| 구조가 없다고 판정됐다 | 군집화는 여기서 끝. `docs/SJH-데이터준비와-모델선정.md` §7-3 의 근거를 이 결과로 교체하고, 모델 쪽은 순서형 회귀·`class_weight`·정규화 튜닝으로 간다 |

각각 별도 계획으로 세운다.
